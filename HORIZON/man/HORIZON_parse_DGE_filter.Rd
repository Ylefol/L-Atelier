% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{HORIZON_parse_DGE_filter}
\alias{HORIZON_parse_DGE_filter}
\title{Filter velocity AnnData using PARSE DGE output and add metadata}
\usage{
HORIZON_parse_DGE_filter(
  velocity_h5ad,
  dge_dir,
  conda_env,
  output_file = NULL,
  force = FALSE
)
}
\arguments{
\item{velocity_h5ad}{Character. Path to the raw \code{adata_vel.h5ad}
produced by \code{\link{HORIZON_run_parse_velocity}}.}

\item{dge_dir}{Character. Path to the split-pipe DGE directory (e.g.
\code{"<output_dir>/combined/all-sample/DGE_filtered"}).
Must contain \code{cell_metadata.csv} and \code{all_genes.csv}.}

\item{conda_env}{Character. Path to the conda environment used for
\code{\link{HORIZON_run_parse_velocity}}.}

\item{output_file}{Character or NULL. Output path for the filtered h5ad.
Defaults to \code{adata_vel_filtered.h5ad} in the same directory as
\code{velocity_h5ad}.}

\item{force}{Logical. Reprocess even if \code{output_file} already exists.}
}
\value{
Character. Path to the filtered h5ad, invisibly.
}
\description{
Takes the raw \code{adata_vel.h5ad} from \code{\link{HORIZON_run_parse_velocity}}
and filters it to the cells and genes present in the split-pipe DGE output
(typically \code{DGE_filtered}), then adds cell and gene metadata from
\code{cell_metadata.csv} and \code{all_genes.csv}.
}
