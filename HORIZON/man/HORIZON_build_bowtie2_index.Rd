% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bowtie2_index.R
\name{HORIZON_build_bowtie2_index}
\alias{HORIZON_build_bowtie2_index}
\title{Build a Bowtie2 genome index}
\usage{
HORIZON_build_bowtie2_index(
  reference,
  index_dir,
  index_name = NULL,
  threads = 4L,
  force = FALSE,
  ...
)
}
\arguments{
\item{reference}{Character vector. Path(s) to reference FASTA file(s).
Multiple FASFTAs are concatenated by bowtie2-build.}

\item{index_dir}{Character. Directory in which to write the index files.
Created automatically if it does not exist.}

\item{index_name}{Character or \code{NULL}. Basename for the index files
(without extension).  Defaults to the filename of
\code{reference[1]} without its extension.}

\item{threads}{Integer. Number of threads passed to \code{--threads}.
Default 4.}

\item{force}{Logical. If \code{TRUE}, rebuild the index even when it
already exists.  Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{Rbowtie2::bowtie2_build}.}
}
\value{
Character. The full index basename path (i.e.
\code{<index_dir>/<index_name>}), invisibly.  Pass this to
\code{index} in \code{\link{HORIZON_run_bowtie2}}.
}
\description{
Wraps \code{Rbowtie2::bowtie2_build()} to create a Bowtie2 index from one
or more reference FASTA files.  The index only needs to be built once per
reference genome; the resulting \code{index_name} path is passed directly
to \code{\link{HORIZON_run_bowtie2}}.
}
