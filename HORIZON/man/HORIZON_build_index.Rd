% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_index.R
\name{HORIZON_build_index}
\alias{HORIZON_build_index}
\title{Build a Rsubread genome index}
\usage{
HORIZON_build_index(
  basename,
  reference,
  memory = 8000,
  index_split = TRUE,
  gapped = TRUE,
  force = FALSE,
  ...
)
}
\arguments{
\item{basename}{Character. Path prefix for the index files
(e.g., \code{"/data/refs/hg38/hg38_subread"}). The directory must already
exist.}

\item{reference}{Character. Path to the reference genome FASTA file.}

\item{memory}{Integer. RAM to use for index building in MB. Default 8000
(~8 GB). Reduce on memory-constrained machines.}

\item{index_split}{Logical. Split the index into chunks to reduce RAM usage
during alignment. Recommended for large genomes. Default TRUE.}

\item{gapped}{Logical. Build a gapped (splice-aware) index suitable for
RNA-seq alignment. Default TRUE.}

\item{force}{Logical. If TRUE, rebuild the index even if one already exists
at \code{basename}. Default FALSE.}

\item{...}{Additional arguments passed to \code{\link[Rsubread]{buildindex}}.}
}
\value{
Character. The index basename path, invisibly.
}
\description{
Wrapper around \code{\link[Rsubread]{buildindex}} for constructing the genome
index required for alignment. This is a one-time operation per reference
genome; the index can be stored on any accessible drive (including external
storage).
}
\details{
RAM note: index building is the most RAM-intensive step. The \code{memory}
parameter directly controls how much RAM Rsubread uses during construction.
With \code{index_split = TRUE} (default), the resulting index is stored in
chunks, which significantly reduces per-alignment RAM usage at the cost of
slightly slower alignment.
}
