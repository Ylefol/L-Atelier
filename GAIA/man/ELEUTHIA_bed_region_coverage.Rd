% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eleuthia_bigwig.R
\name{ELEUTHIA_bed_region_coverage}
\alias{ELEUTHIA_bed_region_coverage}
\title{Compute Coverage for a Genomic Region Directly from a BED File}
\usage{
ELEUTHIA_bed_region_coverage(
  bed_file,
  region,
  chrom_sizes,
  normalize = "CPM",
  bin_size = 10L,
  verbose = FALSE
)
}
\arguments{
\item{bed_file}{Character. Path to the fragment-level BED file (0-based
half-open coordinates: chr, start, end).}

\item{region}{Genomic region. One of:
\itemize{
\item Character string: \code{"chr1:1000000-2000000"}
\item Named character vector: \code{c(chr="chr1", start="1000000", end="2000000")}
\item A \code{GRanges} object (single range)
}}

\item{chrom_sizes}{Exact chromosome lengths. Accepts either:
\itemize{
\item A named numeric vector: \code{c(chr1 = 248956422L, chr2 = 242193529L)}
\item A data.frame with columns \code{chr} and \code{size}, as returned by
\code{APOLLO_get_chromosome_sizes()}.
}}

\item{normalize}{Character. \code{"CPM"} (default) scales by counts per million
total fragments in the file; \code{"raw"} returns absolute counts.}

\item{bin_size}{Integer. Bin width in base pairs. Default \code{10}.}

\item{verbose}{Logical. Print progress messages. Default \code{FALSE}.}
}
\value{
A data.frame with columns \code{start}, \code{end}, \code{score}, \code{midpoint}
covering the requested region. Same format as \code{rtracklayer::import.bw()}
output, making it interchangeable in downstream plotting code.
}
\description{
Extracts fragments overlapping a specific genomic region from
a fragment-level BED file and computes binned coverage — without generating
a full genome-wide BigWig file. Intended as the efficient backend for
\code{\link[=AETHER_plot_coverage_tracks]{AETHER_plot_coverage_tracks()}} when BigWig files are not pre-computed.
}
\details{
The entire BED file is read once (using \code{data.table}) to obtain both the
total fragment count (for CPM) and the region-filtered subset. Only bins
within the requested region are computed — no genome-wide tiling.

Requires: \code{data.table}, \code{GenomicRanges}, \code{IRanges}, \code{GenomeInfoDb}.
}
