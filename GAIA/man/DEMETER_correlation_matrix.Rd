% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demeter_correlation_utils.R
\name{DEMETER_correlation_matrix}
\alias{DEMETER_correlation_matrix}
\title{Calculate correlation matrix with p-values}
\usage{
DEMETER_correlation_matrix(
  data,
  method = "spearman",
  alternative = "two.sided",
  adjust = "none",
  use = "pairwise.complete.obs"
)
}
\arguments{
\item{data}{Data.frame or matrix with variables as columns.}

\item{method}{Correlation method: "spearman", "pearson", or "kendall".
Default: "spearman".}

\item{alternative}{Alternative hypothesis: "two.sided", "less", "greater".
Default: "two.sided".}

\item{adjust}{P-value adjustment method: "BH", "bonferroni", "none", etc.
Default: "none".}

\item{use}{How to handle missing values: "pairwise.complete.obs" (default),
"complete.obs", "everything".}
}
\value{
A list with:
\item{correlation}{Correlation matrix}
\item{pvalue}{P-value matrix}
\item{padj}{Adjusted p-value matrix (if adjust != "none")}
\item{n}{Sample size matrix (pairwise)}
\item{method}{Correlation method used}
}
\description{
Computes pairwise correlations between all columns of a data.frame/matrix,
along with p-values for each correlation.
}
\examples{
\dontrun{
data <- data.frame(a = rnorm(100), b = rnorm(100), c = rnorm(100))
result <- DEMETER_correlation_matrix(data)
result$correlation
result$pvalue

}
}
