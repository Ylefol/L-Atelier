% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_timeseries_plots.R
\name{AETHER_plot_cluster_trajectories}
\alias{AETHER_plot_cluster_trajectories}
\title{Plot Cluster Expression Trajectories}
\usage{
AETHER_plot_cluster_trajectories(
  part_result,
  sample_info,
  time_col = "timepoint",
  group_col = "group",
  norm_counts = NULL,
  clusters = NULL,
  scale_features = FALSE,
  ncol = 4,
  colors = NULL,
  alpha = 0.4,
  title_size = 10
)
}
\arguments{
\item{part_result}{An \code{artemis_part} object from \code{ARTEMIS_part()}.}

\item{sample_info}{Data.frame with sample metadata. Must have rownames
matching colnames of the PART data matrix, and columns for timepoint and
group.}

\item{time_col}{Character. Column in sample_info for timepoint. Default:
"timepoint".}

\item{group_col}{Character. Column in sample_info for group. Default:
"group".}

\item{norm_counts}{Optional matrix of normalized counts (genes x samples).
If NULL, uses the z-scored data from part_result. Default: NULL.}

\item{clusters}{Character vector of clusters to plot (e.g., c("C1", "C3")).
NULL = all non-outlier clusters. Default: NULL.}

\item{scale_features}{Logical. Apply scale feature sum transformation
(value / row_sum) as in TiSA. Recommended when using norm_counts.
Default: FALSE.}

\item{ncol}{Integer. Number of columns in facet grid. Default: 4.}

\item{colors}{Named character vector of colors for groups. NULL = auto.
Default: NULL.}

\item{alpha}{Numeric. Transparency for individual gene lines. Default: 0.4.}

\item{title_size}{Numeric. Font size for facet titles. Default: 10.}
}
\value{
A ggplot object.
}
\description{
Creates faceted line plots showing expression trajectories per PART cluster.
Matches TiSA's visualization style where each panel shows one cluster for one
group, with individual gene lines (thin, colored) and a mean trajectory line
(thick, gray).
}
\details{
This function is ported from TiSA's \code{plot_cluster_traj()} function.
Key features:
\itemize{
\item Faceted by cluster AND group (e.g., "C1 - 50 genes | IgM")
\item Individual gene trajectories as thin colored lines
\item Mean cluster trajectory as thick gray line
\item X-axis shows timepoints, Y-axis shows expression
}
}
\examples{
\dontrun{
AETHER_plot_cluster_trajectories(part_result, sample_info)

}
}
