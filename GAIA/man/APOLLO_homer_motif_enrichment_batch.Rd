% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apollo_motif_enrichment.R
\name{APOLLO_homer_motif_enrichment_batch}
\alias{APOLLO_homer_motif_enrichment_batch}
\title{Run HOMER motif enrichment on multiple BED files}
\usage{
APOLLO_homer_motif_enrichment_batch(
  bed_files,
  genome,
  output_dir,
  ...,
  verbose = TRUE
)
}
\arguments{
\item{bed_files}{Named character vector of BED file paths.
Names are used as peak set identifiers.}

\item{genome}{HOMER genome string (e.g., "hg38").}

\item{output_dir}{Base output directory. Subdirectories are created per peak set.}

\item{...}{Additional arguments passed to APOLLO_homer_motif_enrichment()
(size, mask, denovo, denovo_n, bg, nproc, extra_args).}

\item{verbose}{Logical. Print progress messages. Default TRUE.}
}
\value{
S3 object of class "homer_motif_batch" with components:
\describe{
\item{results}{Named list of homer_motif objects}
\item{combined}{Data.frame of all known motif results with peak_set column}
\item{summary}{Data.frame with per-set summary statistics}
\item{metadata}{List with genome, parameters, bed_files}
}
}
\description{
Iterates over a named vector of BED file paths, running
APOLLO_homer_motif_enrichment() for each and combining results.
}
\examples{
\dontrun{
bed_files <- c(
  up   = "results/peaks_up.bed",
  down = "results/peaks_down.bed"
)
batch <- APOLLO_homer_motif_enrichment_batch(
  bed_files, "hg38", "results/motifs/"
)

}
}
