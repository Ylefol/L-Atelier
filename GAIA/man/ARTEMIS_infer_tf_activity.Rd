% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artemis_decoupler.R
\name{ARTEMIS_infer_tf_activity}
\alias{ARTEMIS_infer_tf_activity}
\title{Infer transcription factor activity}
\usage{
ARTEMIS_infer_tf_activity(
  mat,
  method,
  database = "collectri",
  organism = "human",
  dorothea_levels = c("A", "B", "C", "D", "E"),
  minsize = 5,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{mat}{Numeric matrix of gene expression data. Genes as rows, samples as columns.}

\item{method}{Character. Statistical method to use. REQUIRED - no default.}

\item{database}{Character. TF-target database to use.
Options: "collectri" (default), "dorothea".}

\item{organism}{Character. Organism: "human" or "mouse". Default: "human".}

\item{dorothea_levels}{Character vector. If database = "dorothea", which
confidence levels to include. Default: c("A", "B", "C", "D", "E").}

\item{minsize}{Integer. Minimum targets per TF. Default: 5.}

\item{verbose}{Logical. Print progress. Default: TRUE.}

\item{...}{Additional arguments passed to ARTEMIS_run_decoupler().}
}
\value{
A decoupler_result object with TF activity scores.
}
\description{
Convenience wrapper for TF activity inference. Fetches the appropriate
network and runs activity inference.
}
\examples{
\dontrun{
# Using CollecTRI (default)
tf_activity <- ARTEMIS_infer_tf_activity(expr_matrix, method = "ulm")

# Using DoRothEA with high-confidence regulons
tf_activity <- ARTEMIS_infer_tf_activity(
  expr_matrix,
  method = "mlm",
  database = "dorothea",
  dorothea_levels = c("A", "B")
)

}
}
