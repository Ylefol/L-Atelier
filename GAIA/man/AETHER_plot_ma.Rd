% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_differential_plots.R
\name{AETHER_plot_ma}
\alias{AETHER_plot_ma}
\title{MA Plot for Differential Expression/Accessibility Results}
\usage{
AETHER_plot_ma(
  dea_result,
  label_col = NULL,
  genes_of_interest = NULL,
  label_top_n = 0,
  filter_choice = "padj",
  l2fc_thresh = 1,
  p_thresh = 0.05,
  title = "MA Plot",
  colors = NULL,
  point_size = 0.8,
  point_alpha = 0.7
)
}
\arguments{
\item{dea_result}{A data.frame of DEA results, or a list with a
\code{$results} element (as returned by \code{ARTEMIS_perform_dea()}).
Required columns: \code{baseMean}, \code{log2FoldChange}, and the column
named by \code{filter_choice}.}

\item{label_col}{Character. Column to use for point labels. If NULL,
auto-selects \code{gene_name} if present, otherwise \code{feature_id}.}

\item{genes_of_interest}{Character vector. Values in \code{label_col} to
label. Default: NULL.}

\item{label_top_n}{Integer. Label the top N features by \code{filter_choice}.
Default: 0 (disabled).}

\item{filter_choice}{Column for significance filtering: \code{"padj"} or
\code{"pvalue"}. Default: \code{"padj"}.}

\item{l2fc_thresh}{Numeric. log2 fold change threshold. Default: 1.}

\item{p_thresh}{Numeric. Significance threshold. Default: 0.05.}

\item{title}{Character. Plot title. Default: "MA Plot".}

\item{colors}{Named character vector with colors for \code{"up"},
\code{"down"}, \code{"low_reg"}, \code{"non_sig"}. Default uses
red/blue/green/gray.}

\item{point_size}{Numeric. Point size. Default: 0.8.}

\item{point_alpha}{Numeric. Point transparency. Default: 0.7.}
}
\value{
A ggplot object.
}
\description{
Creates a four-category MA plot (log2 fold change vs log2 mean expression)
with embedded counts in the legend and optional gene labeling.
}
\details{
X-axis is \code{log2(baseMean + 1)}. Y-axis is \code{log2FoldChange}.
A solid red line marks y = 0. Dashed black lines mark \code{±l2fc_thresh}.
Non-significant points are drawn first (background) with significant points
on top.
}
\examples{
\dontrun{
p <- AETHER_plot_ma(dea_result, title = "KO vs WT")

p <- AETHER_plot_ma(dea_result, label_top_n = 10,
                    genes_of_interest = c("SMUG1", "OGG1"))
}
}
