% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_density_scatter.R
\name{AETHER_plot_density_scatter}
\alias{AETHER_plot_density_scatter}
\title{2D Density Scatter Plot}
\usage{
AETHER_plot_density_scatter(
  data,
  x_col = "x",
  y_col = "y",
  x_label = NULL,
  y_label = NULL,
  title = NULL,
  nbins = 100L,
  log_density = FALSE,
  color_low = "white",
  color_high = "darkblue",
  show_zero_lines = FALSE
)
}
\arguments{
\item{data}{data.frame with at least two numeric columns.}

\item{x_col}{Character. Column name for x-axis values. Default \code{"x"}.}

\item{y_col}{Character. Column name for y-axis values. Default \code{"y"}.}

\item{x_label}{Character or \code{NULL}. X-axis label. Falls back to
\code{attr(data, "x_label")} then \code{x_col} if not supplied.}

\item{y_label}{Character or \code{NULL}. Y-axis label. Falls back to
\code{attr(data, "y_label")} then \code{y_col} if not supplied.}

\item{title}{Character or \code{NULL}. Plot title. Default \code{NULL}.}

\item{nbins}{Integer. Number of bins in each dimension. Default 100.}

\item{log_density}{Logical. If \code{TRUE}, apply \code{log1p} to the bin
count colour scale. Useful when a few bins dominate the colour range.
Default \code{FALSE}.}

\item{color_low}{Character. Colour for low-density bins. Default
\code{"white"}.}

\item{color_high}{Character. Colour for high-density bins. Default
\code{"darkblue"}.}

\item{show_zero_lines}{Logical. If \code{TRUE}, draw dashed reference lines
at x = 0 and y = 0. Useful for log2 ratio plots. Default \code{FALSE}.}
}
\value{
A ggplot2 object.
}
\description{
Plots a 2D histogram where color encodes the number of data points in each
bin. Designed to accept output from
\code{\link{ARTEMIS_prepare_bw_comparison}} but works with any data frame
containing two numeric columns.
}
