% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eleuthia_atac_export.R
\name{ELEUTHIA_export_atac_results}
\alias{ELEUTHIA_export_atac_results}
\title{Export ATAC-seq Analysis Results}
\usage{
ELEUTHIA_export_atac_results(
  output_dir,
  annotated_peaks = NULL,
  dea_result = NULL,
  enrichment = NULL,
  homer_result = NULL,
  sequence_composition = NULL,
  prefix = "atac",
  l2fc_thresh = 1,
  p_thresh = 0.05,
  sample_info = NULL,
  group_col = "group",
  save_plots = TRUE,
  plot_format = "png",
  save_sequences = FALSE,
  save_rds = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{output_dir}{Character. Root directory to save all results.
Created if needed.}

\item{annotated_peaks}{Named list of annotated peak data.frames (e.g.,
one entry per group: \code{list(WT = wt_peaks, KO = ko_peaks)}).
Each data.frame is saved as \code{{name}_annotated_peaks.csv} in the
\code{peaks/} subdirectory. Default: NULL.}

\item{dea_result}{DEA result from \code{ARTEMIS_differential_counts()}.
Passed to \code{ELEUTHIA_export_dea_results()}. Default: NULL.}

\item{enrichment}{Enrichment result from \code{APOLLO_enrich_gost()}.
Passed to \code{ELEUTHIA_export_enrichment()}. Default: NULL.}

\item{homer_result}{A \code{homer_motif} or \code{homer_motif_batch}
object. Passed to \code{ELEUTHIA_export_homer_results()}. Default: NULL.}

\item{sequence_composition}{A data.frame from
\code{APOLLO_sequence_composition()}. Passed to
\code{ELEUTHIA_export_sequence_composition()}. Default: NULL.}

\item{prefix}{Character. Filename prefix for DEA, enrichment, HOMER, and
composition exports. Default: "atac".}

\item{l2fc_thresh}{Numeric. log2FC threshold for DEA significance.
Default: 1.0.}

\item{p_thresh}{Numeric. Adjusted p-value threshold for DEA significance.
Default: 0.05.}

\item{sample_info}{Data.frame with sample metadata for DEA heatmap.
Default: NULL.}

\item{group_col}{Character. Group column in \code{sample_info}.
Default: \code{"group"}.}

\item{save_plots}{Logical. Generate and save plots for all result types.
Default: TRUE.}

\item{plot_format}{Character. "png", "pdf", or "both". Default: "png".}

\item{save_sequences}{Logical. Save raw sequences from sequence composition
to a separate file. Default: FALSE.}

\item{save_rds}{Logical. Save full R objects as RDS for all result types.
Default: TRUE.}

\item{verbose}{Logical. Print progress. Default: TRUE.}
}
\value{
Invisible named list of character vectors, one element per
result type exported (e.g., \code{$peaks}, \code{$dea}, \code{$homer}).
Each element contains the file paths created by the corresponding exporter.
}
\description{
Orchestrating export function for the standard ATAC-seq analysis pipeline.
Delegates to dedicated exporters for each result type; any \code{NULL}
argument is silently skipped.
}
\details{
Output subdirectory layout:
\preformatted{
output_dir/
  peaks/                        # annotated peak CSVs + annotation barplot
  dea/                          # DEA results, plots, RDS
    plots/                      # volcano, MA, PART heatmap
  enrichment/                   # enrichment results (always top-level)
  homer/                        # HOMER motifs, plots, RDS
  composition/                  # sequence composition CSV + summary
}
}
\examples{
\dontrun{
ELEUTHIA_export_atac_results(
  output_dir        = "results/atac/",
  annotated_peaks   = list(WT = wt_peaks, KO = ko_peaks),
  dea_result        = dea_res,
  enrichment        = enrich_res,
  homer_result      = homer_batch,
  sequence_composition = comp,
  prefix            = "smug1_atac"
)
}
}
