% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hephaestus_ConvCCA.R
\name{HEPHAESTUS_multi_convCCA}
\alias{HEPHAESTUS_multi_convCCA}
\title{Multi-Dataset Convex Sparse Canonical Correlation Analysis}
\usage{
HEPHAESTUS_multi_convCCA(
  X,
  tau,
  nIter = 1000,
  penalty = "LASSO",
  initW = NULL,
  R = 1
)
}
\arguments{
\item{X}{List of M datasets, where each element is an (n x p_i) matrix.
All datasets must have the same number of samples (rows)}

\item{tau}{List of M sparsity parameters, one per dataset (e.g., list(0.3, 0.5, 0.2))}

\item{nIter}{Maximum number of iterations (default = 1000). Unlike convCCA,
multi.convCCA requires a fixed nIter (no auto-convergence)}

\item{penalty}{Penalty function: "LASSO" (default) or "SCAD"}

\item{initW}{Optional list of M initial canonical vectors}

\item{R}{Number of canonical tuples to compute (default = 1)}
}
\value{
List containing:
\item{W}{List of M canonical vectors, one per dataset}
}
\description{
Performs sparse CCA on multiple (>2) datasets simultaneously,
finding canonical vectors that maximize mutual correlation across all datasets
}
\examples{
\dontrun{
# Simulate 3 correlated datasets
set.seed(123)
n <- 50
u <- matrix(rnorm(n), ncol = 1)
X1 <- u \%*\% t(c(rep(1, 20), rep(0, 80))) + matrix(rnorm(n * 100), n, 100)
X2 <- u \%*\% t(c(rep(1, 15), rep(0, 85))) + matrix(rnorm(n * 100), n, 100)
X3 <- u \%*\% t(c(rep(1, 10), rep(0, 40))) + matrix(rnorm(n * 50), n, 50)

# Run multi.convCCA
result <- HEPHAESTUS_multi_convCCA(
  X = list(X1, X2, X3),
  tau = list(0.3, 0.3, 0.3),
  nIter = 100
)

# Check sparsity for each dataset
sapply(result$W, function(w) sum(abs(w) > 1e-6))

}
}
