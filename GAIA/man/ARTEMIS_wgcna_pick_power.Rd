% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artemis_wgcna.R
\name{ARTEMIS_wgcna_pick_power}
\alias{ARTEMIS_wgcna_pick_power}
\title{Select soft threshold power for WGCNA}
\usage{
ARTEMIS_wgcna_pick_power(
  wgcna_data,
  powers = NULL,
  power_auto = TRUE,
  r2_cutoff = 0.8,
  mean_k_cutoff = NULL,
  network_type = "unsigned",
  plot = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{wgcna_data}{A wgcna_data object, wgcna_cluster object, or expression
matrix (samples as rows).}

\item{powers}{Numeric vector of powers to test. Default: c(1:10, seq(12, 30, by = 2)).}

\item{power_auto}{Logical. If TRUE, automatically select power. If FALSE,
return diagnostics for manual selection. Default: TRUE.}

\item{r2_cutoff}{R-squared threshold for scale-free topology. Power is selected
where R^2 first exceeds this value. Default: 0.80.}

\item{mean_k_cutoff}{Optional. If R^2 criterion yields high mean connectivity,
may select power with lower connectivity. Default: NULL (disabled).}

\item{network_type}{Network type: "unsigned", "signed", or "signed hybrid".
Default: "unsigned".}

\item{plot}{Logical. Generate diagnostic plots. Default: TRUE.}

\item{verbose}{Logical. Print progress messages. Default: TRUE.}
}
\value{
A list with class "wgcna_power" containing:
\item{power}{Selected power (or NA if auto-selection disabled/failed)}
\item{power_estimate}{WGCNA's built-in power estimate}
\item{fit_indices}{Data.frame of fit indices for all tested powers}
\item{selection_method}{How power was selected}
\item{r2_at_power}{R^2 value at selected power}
\item{mean_k_at_power}{Mean connectivity at selected power}
\item{recommendation}{Text recommendation}
}
\description{
Determines the optimal soft threshold power for network construction.
Can automatically select power or return diagnostics for manual selection.
}
\details{
Power selection uses multiple criteria in order:
\enumerate{
\item WGCNA's built-in powerEstimate (if available and meets R^2 threshold)
\item First power where scale-free topology R^2 exceeds r2_cutoff
\item Power with best R^2 if none exceed threshold (with warning)
}

The diagnostic plots show:
\itemize{
\item Scale-free topology fit (R^2) vs power
\item Mean connectivity vs power
}
}
\examples{
\dontrun{
wgcna_data <- ARTEMIS_wgcna_prepare(counts, traits)
power_result <- ARTEMIS_wgcna_pick_power(wgcna_data)
# Use: power_result$power

# Manual selection (just show plots, don't auto-select)
power_result <- ARTEMIS_wgcna_pick_power(wgcna_data, power_auto = FALSE)

}
}
