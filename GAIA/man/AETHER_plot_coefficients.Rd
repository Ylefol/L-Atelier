% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_coefficient_plots.R
\name{AETHER_plot_coefficients}
\alias{AETHER_plot_coefficients}
\title{Forest Plot of Regression Coefficients}
\usage{
AETHER_plot_coefficients(
  coef_table,
  null_line = 1,
  log_scale = TRUE,
  sort_by = "estimate",
  decreasing = TRUE,
  colors = NULL,
  point_size = 3,
  line_width = 0.8,
  text_size = 11,
  title = "Forest Plot",
  xlab = NULL,
  show_values = TRUE,
  sig_level = 0.05
)
}
\arguments{
\item{coef_table}{A data.frame from \code{ARTEMIS_extract_coefficients()},
or any data.frame with columns: variable, estimate, ci_lower, ci_upper.}

\item{null_line}{Numeric. Reference line value (default = 1 for odds ratios,
use 0 for raw coefficients). Set to NULL to omit.}

\item{log_scale}{Logical. Use log scale for x-axis (default = TRUE for odds
ratios, FALSE for coefficients).}

\item{sort_by}{Character. How to sort variables:
\itemize{
\item "estimate": By effect size (default)
\item "significance": By p-value
\item "name": Alphabetically
\item "none": Keep original order
}}

\item{decreasing}{Logical. Sort direction (default = TRUE, largest first).}

\item{colors}{Named vector or character. Colors for significant/non-significant:
\itemize{
\item Default: c(significant = "#E41A1C", nonsignificant = "#377EB8")
\item Or single color for all points
}}

\item{point_size}{Numeric. Size of point estimates (default = 3).}

\item{line_width}{Numeric. Width of confidence interval lines (default = 0.8).}

\item{text_size}{Numeric. Base text size (default = 11).}

\item{title}{Character. Plot title (default = "Forest Plot").}

\item{xlab}{Character. X-axis label (default based on log_scale).}

\item{show_values}{Logical. Show estimate values on right side (default = TRUE).}

\item{sig_level}{Numeric. Significance threshold for coloring (default = 0.05).}
}
\value{
A ggplot object that can be further customized or saved.
}
\description{
Creates a forest plot displaying coefficients (or odds ratios)
with confidence intervals. Standard visualization for presenting regression
results in clinical research.
}
\details{
Forest plots are the standard way to present regression coefficients in
medical/clinical research. Key features:
\itemize{
\item Each row = one variable
\item Point = effect estimate (odds ratio or coefficient)
\item Horizontal line = confidence interval
\item Vertical reference line at null value (1 for OR, 0 for coef)
}

Interpretation for odds ratios:
\itemize{
\item OR > 1 and CI doesn't cross 1: Significant positive association
\item OR < 1 and CI doesn't cross 1: Significant negative association
\item CI crosses 1: Not statistically significant
}
}
\examples{
\dontrun{
# Basic forest plot of odds ratios
coef_table <- ARTEMIS_extract_coefficients(final_model, verbose = FALSE)
AETHER_plot_coefficients(coef_table)

# For raw coefficients
coef_table <- ARTEMIS_extract_coefficients(final_model, format = "coefficient",
                                            verbose = FALSE)
AETHER_plot_coefficients(coef_table, null_line = 0, log_scale = FALSE)

# Sort by p-value
AETHER_plot_coefficients(coef_table, sort_by = "significance")

}
}
