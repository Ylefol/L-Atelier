% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_cluster_plots.R
\name{AETHER_plot_cluster_sankey}
\alias{AETHER_plot_cluster_sankey}
\title{Plot Sankey Diagram for Cluster Transitions Across k Values}
\usage{
AETHER_plot_cluster_sankey(
  k_eval,
  colors = NULL,
  node_width = 30,
  font_size = 12,
  title = "Cluster Transitions Across k",
  save_html = NULL
)
}
\arguments{
\item{k_eval}{An artemis_k_evaluation object from ARTEMIS_evaluate_k_range().}

\item{colors}{Named character vector or NULL. Custom colors for clusters.
If NULL (default), uses a generated palette.}

\item{node_width}{Numeric. Width of cluster nodes. Default = 30.}

\item{font_size}{Numeric. Font size for labels. Default = 12.}

\item{title}{Character. Plot title. Default = "Cluster Transitions Across k".}

\item{save_html}{Character. If provided, saves plot as standalone HTML file.
Default = NULL (no save).}
}
\value{
A plotly/networkD3 Sankey diagram object
}
\description{
Visualize how samples flow between clusters as k increases.
This helps understand cluster stability and identify natural substructure.
}
\details{
The Sankey diagram shows:
\itemize{
\item Columns: different k values (left to right)
\item Nodes: clusters at each k value
\item Flows: how samples move from clusters at k to clusters at k+1
\item Width: number of samples in each flow
}

Interpretation:
\itemize{
\item Clean splits (one cluster splits into two) suggest natural substructure
\item Messy redistributions suggest artificial divisions
\item Stable cores (samples staying together) indicate robust clusters
}
}
\examples{
\dontrun{
k_eval <- ARTEMIS_evaluate_k_range(my_data, k_range = 2:5)
AETHER_plot_cluster_sankey(k_eval)

# Save as HTML for sharing
AETHER_plot_cluster_sankey(k_eval, save_html = "cluster_transitions.html")

}
}
