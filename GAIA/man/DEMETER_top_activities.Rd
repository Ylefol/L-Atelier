% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demeter_result_loaders.R
\name{DEMETER_top_activities}
\alias{DEMETER_top_activities}
\title{Select top N sources from activity results}
\usage{
DEMETER_top_activities(result, n = 20, by = "variance", verbose = TRUE)
}
\arguments{
\item{result}{A decoupler_result object, or a matrix of activities
(sources as rows, samples as columns).}

\item{n}{Integer. Number of top sources to keep. Default: 20.}

\item{by}{Character. Criterion for selecting top sources:
\itemize{
\item "variance": highest variance across samples (most variable)
\item "activity": highest mean absolute activity (strongest signals)
\item "max": highest maximum absolute activity
Default: "variance".
}}

\item{verbose}{Logical. Print selection info. Default: TRUE.}
}
\value{
If input was decoupler_result, returns filtered decoupler_result.
If input was matrix, returns filtered matrix.
}
\description{
Filters activity results to keep only the top N sources (TFs/pathways)
based on variance, mean absolute activity, or other criteria.
}
\details{
Use this to reduce the number of TFs/pathways before:
\itemize{
\item Plotting heatmaps (too many rows are unreadable
\item Using as traits in WGCNA module-trait correlation
\item Any analysis where you want to focus on the most informative sources
}
}
\examples{
\dontrun{
# Filter to top 20 most variable TFs
tf_top <- DEMETER_top_activities(tf_result, n = 20, by = "variance")

# Use with WGCNA
tf_trait_cor <- ARTEMIS_wgcna_module_traits(
  modules = modules,
  traits = t(tf_top$activities)
)

# Filter to top 30 by activity strength
tf_top <- DEMETER_top_activities(tf_result, n = 30, by = "activity")

}
}
