% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apollo_activity_networks.R
\name{APOLLO_get_dorothea}
\alias{APOLLO_get_dorothea}
\title{Get DoRothEA TF regulons}
\usage{
APOLLO_get_dorothea(
  organism = "human",
  levels = c("A", "B", "C", "D", "E"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{organism}{Character. Organism to use: "human" or "mouse". Default: "human".}

\item{levels}{Character vector. Confidence levels to include.
Options: "A" (highest), "B", "C", "D", "E" (lowest).
Default: c("A", "B", "C", "D", "E") (all levels).}

\item{verbose}{Logical. Print information about the network. Default: TRUE.}

\item{...}{Additional arguments passed to decoupleR::get_dorothea().}
}
\value{
A tibble with columns:
\item{source}{Transcription factor name}
\item{target}{Target gene name}
\item{mor}{Mode of regulation: 1 (activation) or -1 (repression)}
\item{confidence}{Confidence level (A-E)}
}
\description{
Retrieves DoRothEA transcription factor regulons with confidence level
filtering. DoRothEA provides curated TF-target interactions with
assigned confidence scores.
}
\details{
DoRothEA confidence levels:
\itemize{
\item A: Highest confidence (multiple evidence types)
\item B: High confidence (literature + ChIP-seq)
\item C: Medium confidence (ChIP-seq or inference)
\item D: Low confidence (inference only)
\item E: Lowest confidence (predicted)
}

For most analyses, levels A-C are recommended. All levels are returned
by default to allow downstream filtering (e.g., at visualization).
}
\examples{
\dontrun{
# Get all DoRothEA regulons
network <- APOLLO_get_dorothea()

# Get only high-confidence regulons
network_hc <- APOLLO_get_dorothea(levels = c("A", "B"))

}
}
