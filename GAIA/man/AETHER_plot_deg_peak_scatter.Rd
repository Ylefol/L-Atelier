% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_differential_plots.R
\name{AETHER_plot_deg_peak_scatter}
\alias{AETHER_plot_deg_peak_scatter}
\title{Scatter Plot of DEG LFC vs Distance to Nearest ATAC Peak}
\usage{
AETHER_plot_deg_peak_scatter(
  deg_peaks,
  color_up = "#D6604D",
  color_down = "#2166AC",
  alpha = 0.7,
  size_range = c(1, 5),
  label_top = 10,
  label_col = NULL,
  show_tss_line = TRUE,
  show_lfc_line = TRUE,
  title = NULL,
  x_limits = NULL
)
}
\arguments{
\item{deg_peaks}{An \code{apollo_deg_peaks} object from
\code{APOLLO_link_degs_to_peaks()}.}

\item{color_up}{Character. Colour for upregulated DEGs.
Default \code{"#D6604D"} (red).}

\item{color_down}{Character. Colour for downregulated DEGs.
Default \code{"#2166AC"} (blue).}

\item{alpha}{Numeric. Point transparency. Default \code{0.7}.}

\item{size_range}{Numeric vector of length 2. Min and max point sizes
(mapped to -log10 padj). Default \code{c(1, 5)}.}

\item{label_top}{Integer. Number of top genes (by padj) to label with
\code{ggrepel}. Default \code{10}. Set to \code{0} to suppress labels.}

\item{label_col}{Character. Column in \code{$linked} used for labels.
Default \code{NULL} (uses the \code{gene_col} from the linking step).}

\item{show_tss_line}{Logical. Draw a vertical dashed line at distance = 0
(the TSS). Default \code{TRUE}.}

\item{show_lfc_line}{Logical. Draw a horizontal dashed line at LFC = 0.
Default \code{TRUE}.}

\item{title}{Character. Plot title. Default \code{NULL} (auto-generated).}

\item{x_limits}{Numeric vector of length 2 or \code{NULL}. X-axis limits
in bp. Default \code{NULL} (auto from data).}
}
\value{
A \code{ggplot} object.
}
\description{
Visualises the output of \code{APOLLO_link_degs_to_peaks()} as
a scatter plot of log2 fold change against signed distance to the TSS
(negative = upstream, positive = downstream). Each point is one peak-DEG
pair. Point size encodes statistical significance (-log10 padj); colour
encodes expression direction.
}
