% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_enrichment_plots.R
\name{AETHER_plot_gsea_enrichment}
\alias{AETHER_plot_gsea_enrichment}
\title{Running-Score (Mountain) Plot for a Single GSEA Pathway}
\usage{
AETHER_plot_gsea_enrichment(gsea_result, pathway, title = NULL)
}
\arguments{
\item{gsea_result}{An \code{apollo_gsea} object from \code{APOLLO_gsea()}.
Must retain \code{$gene_sets} and \code{$ranked_genes} (both included by
default).}

\item{pathway}{Character. Name of the gene set to plot (must be present in
\code{gsea_result$gene_sets}).}

\item{title}{Character or \code{NULL}. Plot title. \code{NULL} (default)
uses the pathway name.}
}
\value{
A ggplot object.
}
\description{
Wraps \code{fgsea::plotEnrichment()} to draw the running
enrichment score curve for one gene set from a GSEA result
(\code{APOLLO_gsea()}).
}
\examples{
\dontrun{
gsea_result <- APOLLO_gsea(ranked, collection = "H")
p <- AETHER_plot_gsea_enrichment(gsea_result, "HALLMARK_INFLAMMATORY_RESPONSE")
}
}
