% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_scca_plots.R
\name{AETHER_plot_multi_feature_weights}
\alias{AETHER_plot_multi_feature_weights}
\title{Plot Feature Weights from Multi-Dataset Sparse CCA}
\usage{
AETHER_plot_multi_feature_weights(
  weights_list,
  method_name = "sCCA",
  dataset_names = NULL,
  true_features_list = NULL,
  threshold = 1e-06
)
}
\arguments{
\item{weights_list}{List of numeric vectors, each containing canonical weights for one dataset.
Length of list = number of datasets integrated.}

\item{method_name}{Character string for plot title (default = "sCCA")}

\item{dataset_names}{Optional character vector of dataset names for panel labels.
If NULL, uses "Dataset 1", "Dataset 2", etc.}

\item{true_features_list}{Optional list of numeric vectors, each c(start, end) indicating
true signal region for that dataset. NULL elements mean no highlighting for that dataset.
Useful for simulated data validation.}

\item{threshold}{Threshold for considering weights as non-zero (default = 1e-6)}
}
\value{
ggplot2 object with faceted panels. Also prints summary statistics as side effect.
}
\description{
Creates faceted stem plots showing canonical weights from multi-dataset sCCA.
Each dataset gets its own panel. Highlights non-zero features and optionally shows
true signal regions. Prints summary statistics for each dataset.
}
\details{
Creates faceted stem plots (one panel per dataset) where:
\itemize{
\item Non-zero weights (|weight| > threshold) are shown in orange
\item Zero/negligible weights are shown in gray
\item True signal regions (if specified) are highlighted with blue background
\item Each panel shows one dataset's canonical weights
\item Prints for each dataset: number of non-zero features, max weight, mean absolute weight
}
}
\examples{
\dontrun{
# Simulate multi-dataset sCCA results
weights1 <- c(rnorm(20, mean = 0.5), rep(0, 80))
weights2 <- c(rnorm(15, mean = 0.6), rep(0, 85))
weights3 <- c(rnorm(10, mean = 0.4), rep(0, 40))

# Basic plot
p <- AETHER_plot_multi_feature_weights(
  weights_list = list(weights1, weights2, weights3),
  method_name = "multi.convCCA",
  dataset_names = c("RNA-seq", "ATAC-seq", "CUT&TAG")
)

# Plot with true signal regions highlighted
p <- AETHER_plot_multi_feature_weights(
  weights_list = list(weights1, weights2, weights3),
  method_name = "multi.convCCA",
  dataset_names = c("RNA-seq", "ATAC-seq", "CUT&TAG"),
  true_features_list = list(c(1, 20), c(1, 15), c(1, 10))
)

}
}
