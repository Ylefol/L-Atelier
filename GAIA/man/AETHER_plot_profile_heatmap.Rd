% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_profile_heatmap.R
\name{AETHER_plot_profile_heatmap}
\alias{AETHER_plot_profile_heatmap}
\title{Plot Signal Profile Heatmap (scale-regions layout)}
\usage{
AETHER_plot_profile_heatmap(
  bigwig_files,
  txdb,
  genes = NULL,
  peaks = NULL,
  window = 3000L,
  bin_size = 50L,
  body_bins = 100L,
  color_low = "#FFF7EC",
  color_high = "#7F0000",
  profile_color = "#2166AC",
  cap_quantile = 0.99,
  cap_val = NULL,
  profile_ylim = NULL,
  max_genes = 5000L,
  profile_height = 1,
  heatmap_height = 5,
  title = NULL,
  verbose = TRUE
)
}
\arguments{
\item{bigwig_files}{Named list of BigWig file paths. Each element is a
character vector. If multiple files are supplied they are averaged into
one track. Element names become the panel labels.
Example: \code{list(WT = c("wt1.bw","wt2.bw"), KO = "ko.bw")}}

\item{txdb}{TxDb object (from \code{\link[=APOLLO_make_txdb]{APOLLO_make_txdb()}}). Used to extract TSS
and TES positions for all (or selected) genes.}

\item{genes}{Optional character vector of gene identifiers to include. If
\code{NULL} (default) all genes in \code{txdb} are used. Matched against raw TxDb
gene IDs and NCBI "gene-" stripped forms.}

\item{peaks}{Optional \code{GRanges} object or path to a BED/narrowPeak file of
called peaks (e.g. from ATAC or ChIP). When supplied, only genes whose
extended region (body ± \code{window}) overlaps at least one peak are retained.
This is more principled than the \code{max_genes} heuristic: rather than
guessing the top-N genes by TSS signal, you provide the exact loci of
interest. \code{max_genes} still acts as a hard cap on the result.}

\item{window}{Integer. Base pairs upstream of TSS and downstream of TES to
include as flanking regions. Default \code{3000}.}

\item{bin_size}{Integer. Bin width in bp for the flanking regions. The
number of flank bins on each side = \code{ceiling(window / bin_size)}.
Default \code{50}.}

\item{body_bins}{Integer. Number of bins the gene body is scaled to,
regardless of gene length. Default \code{100}.}

\item{color_low}{Character. Fill colour for zero/low signal.
Default \code{"#FFF7EC"} (near-white cream).}

\item{color_high}{Character. Fill colour for maximum signal.
Default \code{"#7F0000"} (dark red).}

\item{profile_color}{Character. Line and fill colour for the profile curve.
Default \code{"#2166AC"} (blue).}

\item{cap_quantile}{Numeric in (0,1). Signal values above this quantile are
capped before display (shared cap across all panels). Default \code{0.99}.}

\item{cap_val}{Optional numeric. Fixed colour-scale cap value shared across
calls (e.g. a mark-level global cap). When \code{NULL} (default) the cap is
computed per-call from \code{cap_quantile}.}

\item{profile_ylim}{Optional numeric. Fixed y-axis upper limit for the mean
signal profile, shared across calls. When \code{NULL} (default) the limit is
computed per-call from the data.}

\item{max_genes}{Integer or \code{NULL}. Hard cap on the number of genes
displayed. When \code{peaks} is supplied this limits the peak-filtered set;
when \code{peaks} is \code{NULL} the TSS-signal pre-filter keeps the top
\code{max_genes} genes. Set \code{NULL} to disable. Default \code{5000}.}

\item{profile_height}{Numeric. Relative height of the profile panel.
Default \code{1}.}

\item{heatmap_height}{Numeric. Relative height of the heatmap panel.
Default \code{5}.}

\item{title}{Optional character string. Overall figure title.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{patchwork} composite plot.
}
\description{
Produces a deepTools \code{computeMatrix scale-regions}-style
figure. Each panel shows a mean signal profile curve above a row-sorted
heatmap. The x-axis spans from \code{window} bp upstream of the TSS through
the scaled gene body to \code{window} bp downstream of the TES. The gene
body is scaled to \code{body_bins} equal bins regardless of gene length, so
genes of different sizes are directly comparable. Rows are sorted by
descending total signal (highest at top). Multiple samples are displayed
side-by-side in one composite figure.
}
\details{
The gene body is scaled independently per gene: a 500 bp gene and a 50 kb
gene both contribute \code{body_bins} columns to the matrix. The flanking
regions use a fixed \code{bin_size}. Minus-strand genes are flipped so that
upstream is always on the left. TSS and TES positions are marked with dashed
vertical lines on both the profile and heatmap panels.

Requires: \code{GenomicRanges}, \code{IRanges}, \code{S4Vectors}, \code{rtracklayer},
\code{GenomicFeatures}, \code{EnrichedHeatmap}, \code{patchwork}.
}
