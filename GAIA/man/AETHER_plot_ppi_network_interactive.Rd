% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_network_plots.R
\name{AETHER_plot_ppi_network_interactive}
\alias{AETHER_plot_ppi_network_interactive}
\title{Interactive PPI Network Plot}
\usage{
AETHER_plot_ppi_network_interactive(
  graph,
  color_by = "degree",
  size_by = "degree",
  colors = NULL,
  size_range = c(10, 40),
  show_labels = TRUE,
  physics = TRUE,
  layout = "layout_with_fr",
  group_labels = NULL,
  title = NULL,
  save_html = NULL,
  seed = 42
)
}
\arguments{
\item{graph}{An igraph object (e.g., from APOLLO_get_ppi()).}

\item{color_by}{Node coloring. Same options as AETHER_plot_ppi_network():
named vector, "degree", "betweenness", single color, or NULL.}

\item{size_by}{Node sizing. Same options as AETHER_plot_ppi_network().}

\item{colors}{Color specification. Named vector for discrete, or gradient
vector for continuous. Default: NULL (auto-selected).}

\item{size_range}{Numeric vector of length 2. Min and max node sizes.
Default: c(10, 40).}

\item{show_labels}{Logical. Show node labels. Default: TRUE.}

\item{physics}{Logical. Enable physics simulation for layout. Default: TRUE.
Set FALSE for static layout (faster for large networks).}

\item{layout}{Character. Layout algorithm when physics=FALSE.
Options: "layout_with_fr", "layout_nicely", "layout_in_circle".
Default: "layout_with_fr".}

\item{group_labels}{Named character vector. Maps a discrete \code{color_by}
group (e.g. PART cluster "C1", "C2", ...) to a label string (e.g. its
top enriched GO term from \code{APOLLO_enrich_gost()}). Added to each
matching node's hover tooltip as "Cluster" and "Top term" lines (no
separate legend is drawn — see \code{\link{AETHER_plot_ppi_network}}'s
static \code{group_labels} for that). Only applies when \code{color_by}
produces a discrete grouping — ignored with a warning otherwise.
Default: NULL.}

\item{title}{Character. Plot title. Default: NULL.}

\item{save_html}{Character. Path to save as standalone HTML file. Default: NULL.}

\item{seed}{Integer. Random seed for layout. Default: 42.}
}
\value{
A visNetwork object (rendered in viewer or saved to HTML).
}
\description{
Creates an interactive network visualization using visNetwork.
Nodes can be dragged, zoomed, and hovered for information. Useful for
exploring large networks.
}
\examples{
\dontrun{
# Interactive exploration
AETHER_plot_ppi_network_interactive(graph, color_by = "degree")

# Save to HTML
AETHER_plot_ppi_network_interactive(graph, save_html = "network.html")

# Show cluster + top enriched term in each node's tooltip
top_terms <- c(C1 = "immune response", C2 = "coagulation cascade")
AETHER_plot_ppi_network_interactive(graph, color_by = clusters,
                                     group_labels = top_terms)

}
}
