% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demeter_result_loaders.R
\name{DEMETER_select_circos_modules}
\alias{DEMETER_select_circos_modules}
\title{Select WGCNA modules for circos display}
\usage{
DEMETER_select_circos_modules(
  circos_data,
  enrichment = NULL,
  min_cor = 0.3,
  require_sig_cor = TRUE,
  min_size = 30,
  min_overlap = 1,
  exclude_grey = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{circos_data}{A \code{part_wgcna_circos} object.}

\item{enrichment}{Optional. Enrichment results from \code{APOLLO_enrich_gost()}
or similar. Must have a \code{module} column. Modules with at least one
significant term pass the filter.}

\item{min_cor}{Numeric. Minimum absolute trait correlation with at least one
trait. Default: 0.3. Set to 0 to disable.}

\item{require_sig_cor}{Logical. Require at least one significant trait
correlation (based on the _sig columns in module_df). Default: TRUE.
Ignored if no trait data is available.}

\item{min_size}{Integer. Minimum module size (gene count). Default: 30.}

\item{min_overlap}{Integer. Minimum number of genes overlapping with any
PART cluster (from the association matrix). Default: 1.}

\item{exclude_grey}{Logical. Exclude the grey (unassigned) module. Default: TRUE.}

\item{verbose}{Logical. Print filtering summary. Default: TRUE.}
}
\value{
Character vector of module names passing all criteria.
}
\description{
Identifies modules worth displaying in the circos plot based on multiple
criteria: trait correlation significance and strength, module size, overlap
with PART clusters, and (optionally) enrichment results. Returns module
names suitable for passing to \code{DEMETER_filter_circos_modules()}.
}
\details{
All enabled criteria are combined with AND logic: a module must pass every
applicable filter. Criteria are skipped when their data is unavailable
(e.g., enrichment filtering is skipped if \code{enrichment = NULL}).
}
\examples{
\dontrun{
selected <- DEMETER_select_circos_modules(circos_data, min_cor = 0.4)
circos_data <- DEMETER_filter_circos_modules(circos_data, selected)

}
}
