% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apollo_gene_enrichment.R
\name{APOLLO_filter_enrichment}
\alias{APOLLO_filter_enrichment}
\title{Filter enrichment results by source or module}
\usage{
APOLLO_filter_enrichment(
  enrich_result,
  sources = NULL,
  modules = NULL,
  top_n = NULL
)
}
\arguments{
\item{enrich_result}{A gost_enrichment object from APOLLO_enrich_gost().}

\item{sources}{Character vector of sources to keep (e.g., c("GO:BP", "KEGG")).
Default: NULL (all sources).}

\item{modules}{Character vector of modules to keep. Default: NULL (all modules).}

\item{top_n}{Integer. Keep only top N terms per module per source. Default: NULL (all).}
}
\value{
Filtered data.frame of enrichment results.
}
\description{
Extract specific subsets from a gost_enrichment result object.
}
\examples{
\dontrun{
# Get only KEGG results from blue module
kegg_blue <- APOLLO_filter_enrichment(results, sources = "KEGG", modules = "blue")

# Get top 10 GO:BP terms per module
top_bp <- APOLLO_filter_enrichment(results, sources = "GO:BP", top_n = 10)

}
}
