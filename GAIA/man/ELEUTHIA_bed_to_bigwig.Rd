% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eleuthia_bigwig.R
\name{ELEUTHIA_bed_to_bigwig}
\alias{ELEUTHIA_bed_to_bigwig}
\title{Convert Fragment-Level BED to BigWig}
\usage{
ELEUTHIA_bed_to_bigwig(
  bed_file,
  output_path,
  chrom_sizes,
  normalize = "CPM",
  bin_size = 10L,
  chromosomes = NULL,
  verbose = TRUE
)
}
\arguments{
\item{bed_file}{Character. Path to the fragment-level BED file (0-based
half-open coordinates: chr, start, end).}

\item{output_path}{Character. Output file path. Extension should be \code{.bw}
or \code{.bigwig}; format is inferred by \code{rtracklayer} from the extension.}

\item{chrom_sizes}{Exact chromosome lengths. Accepts either:
\itemize{
\item A named numeric vector: \code{c(chr1 = 248956422L, chr2 = 242193529L)}
\item A data.frame with columns \code{chr} and \code{size}, as returned by
\code{APOLLO_get_chromosome_sizes()}.
Names/values must match chromosome names in the BED file exactly.
No inferring from data.
}}

\item{normalize}{Character. Normalization method: \code{"CPM"} (default) scales
signal by counts per million total fragments; \code{"raw"} leaves coverage as
absolute fragment counts.}

\item{bin_size}{Integer. Bin width in base pairs for coverage aggregation.
Larger values produce smoother signal and smaller files. Default \code{10}.}

\item{chromosomes}{Character vector. Restrict processing to these
chromosomes. Default \code{NULL} processes all chromosomes present in both
the BED file and \code{chrom_sizes}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
Invisibly returns \code{output_path}.
}
\description{
Converts a fragment-level BED file (one row per fragment) into
a BigWig coverage file suitable for genome browser visualization or use with
\code{\link[=AETHER_plot_coverage_tracks]{AETHER_plot_coverage_tracks()}}. Coverage is computed at a configurable bin
resolution and optionally normalized to CPM.
}
\details{
BED files use 0-based half-open coordinates. These are converted to
1-based closed intervals for GRanges internally.

Requires Bioconductor packages: \code{GenomicRanges}, \code{IRanges},
\code{GenomeInfoDb}, \code{rtracklayer}. Install via \code{BiocManager::install()}.
}
