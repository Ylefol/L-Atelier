% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_pca_qc.R
\name{AETHER_plot_pc_association}
\alias{AETHER_plot_pc_association}
\title{PC-Metadata Association Heatmap}
\usage{
AETHER_plot_pc_association(
  assoc_result,
  p_threshold = 0.05,
  top_n = NULL,
  order_vars = TRUE,
  max_log10p = NULL,
  color_low = "white",
  color_high = "#b2182b",
  show_values = FALSE,
  font_size = 9,
  title = "PC-Metadata Association"
)
}
\arguments{
\item{assoc_result}{An \code{artemis_pc_assoc} object from
\code{ARTEMIS_pc_metadata_association()}.}

\item{p_threshold}{Numeric. Significance threshold for the cell border
annotation. Default: 0.05.}

\item{top_n}{Integer or NULL. If set, retains only the \code{top_n} most
significant variables (lowest p-value) per PC, then shows the union across
all PCs. Useful when many variables are significant. NULL = show all
tested variables. Default: NULL.}

\item{order_vars}{Logical. Order metadata variables (columns) by mean
-log10(p) descending so the most associated variables appear first.
Default: TRUE.}

\item{max_log10p}{Numeric or NULL. Cap the colour scale at this value.
NULL = use the data maximum. Default: NULL.}

\item{color_low}{Character. Colour for low -log10(p) (no association).
Default: "white".}

\item{color_high}{Character. Colour for high -log10(p) (strong association).
Default: "#b2182b" (red).}

\item{show_values}{Logical. Overlay -log10(p) values as text in each cell.
Default: FALSE.}

\item{font_size}{Numeric. Base font size. Default: 9.}

\item{title}{Character or NULL. Plot title. Default: "PC-Metadata Association".}
}
\value{
A ggplot object.
}
\description{
Heatmap of -log10(p-values) from a PC-metadata association
test. Rows are principal components, columns are metadata variables. The
colour intensity indicates strength of association; a dashed line marks
the significance threshold.
}
\examples{
\dontrun{
assoc <- ARTEMIS_pc_metadata_association(ol$wide, ol$sample_meta,
                                          sample_col = "SampleID")
p <- AETHER_plot_pc_association(assoc)
print(p)
}
}
