% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hades_olink.R
\name{HADES_filter_olink_proteins}
\alias{HADES_filter_olink_proteins}
\title{Filter Olink Proteins by Missingness}
\usage{
HADES_filter_olink_proteins(olink_data, max_na_fraction = 0.2, verbose = TRUE)
}
\arguments{
\item{olink_data}{An \code{olink_data} object from \code{ELEUTHIA_load_olink()}
or \code{HADES_filter_olink()}.}

\item{max_na_fraction}{Numeric (0-1). Maximum allowable proportion of NA
NPX values across samples for a protein to be retained. Proteins with
NA fraction above this threshold are removed. Default: 0.2 (20\%).}

\item{verbose}{Logical. Print filtering summary. Default: TRUE.}
}
\value{
A filtered \code{olink_data} object. \code{$wide}, \code{$data},
and \code{$assay_meta} contain only the retained proteins.
}
\description{
Removes proteins (rows) from an \code{olink_data} object whose
NPX missingness fraction across samples exceeds a threshold. High-NA
proteins are dropped because missingness cannot be reliably attributed to
a true zero signal versus technical absence.
}
\examples{
\dontrun{
ol <- HADES_filter_olink(ol_raw)
ol <- HADES_filter_olink_proteins(ol, max_na_fraction = 0.2)
}
}
