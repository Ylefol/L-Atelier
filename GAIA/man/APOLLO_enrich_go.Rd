% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apollo_peak_annotation.R
\name{APOLLO_enrich_go}
\alias{APOLLO_enrich_go}
\title{GO Enrichment Analysis for Annotated Peaks}
\usage{
APOLLO_enrich_go(
  annotated_peaks,
  org_db = "org.Hs.eg.db",
  ont = "BP",
  gene_id_type = "ENTREZID",
  pval_cutoff = 0.05,
  qval_cutoff = 0.1,
  min_gs_size = 10,
  max_gs_size = 500,
  verbose = TRUE
)
}
\arguments{
\item{annotated_peaks}{Data.frame from APOLLO_annotate_peaks() with gene_id column.
Alternatively, a character vector of gene IDs.}

\item{org_db}{Character string or OrgDb object. The organism annotation database.
Default is "org.Hs.eg.db" for human.}

\item{ont}{Character. GO ontology: "BP" (Biological Process), "MF" (Molecular
Function), "CC" (Cellular Component), or "ALL" (default = "BP").}

\item{gene_id_type}{Character. Type of gene IDs provided. One of "ENTREZID",
"ENSEMBL", "SYMBOL", "REFSEQ" (default = "ENTREZID").}

\item{pval_cutoff}{Numeric. P-value cutoff for enrichment (default = 0.05).}

\item{qval_cutoff}{Numeric. Adjusted p-value cutoff (default = 0.1).}

\item{min_gs_size}{Integer. Minimum gene set size (default = 10).}

\item{max_gs_size}{Integer. Maximum gene set size (default = 500).}

\item{verbose}{Logical. Print summary (default = TRUE).}
}
\value{
An enrichResult object from clusterProfiler. Key methods:
\describe{
\item{as.data.frame()}{Convert to data.frame of results}
\item{dotplot()}{Create dot plot of top terms}
\item{barplot()}{Create bar plot of top terms}
\item{cnetplot()}{Gene-concept network plot}
}
}
\description{
Performs Gene Ontology enrichment analysis on genes associated
with annotated peaks using clusterProfiler.
}
\details{
If gene IDs are not ENTREZID, the function attempts to convert them using
the org.Db. This is necessary because GO enrichment requires ENTREZ IDs.

For T2T-CHM13 or other genomes, gene symbols should still work with
org.Hs.eg.db since the gene names are the same.
}
\examples{
\dontrun{
# From annotated peaks
annotated <- APOLLO_annotate_peaks(sig_peaks, txdb)
go_results <- APOLLO_enrich_go(annotated)

# View results
head(as.data.frame(go_results))

# Plot
dotplot(go_results, showCategory = 20)

# From gene vector
genes <- c("TP53", "BRCA1", "EGFR")
go_results <- APOLLO_enrich_go(genes, gene_id_type = "SYMBOL")

}
}
