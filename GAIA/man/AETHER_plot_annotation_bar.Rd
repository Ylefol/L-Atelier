% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_enrichment_plots.R
\name{AETHER_plot_annotation_bar}
\alias{AETHER_plot_annotation_bar}
\title{Genomic Annotation Distribution Bar Plot}
\usage{
AETHER_plot_annotation_bar(
  annotated_list,
  use_simple = TRUE,
  show_percentage = TRUE,
  colors = NULL,
  title = "Genomic Feature Distribution",
  bar_height = 0.7,
  show_labels = TRUE,
  min_label_pct = 5
)
}
\arguments{
\item{annotated_list}{A named list of data.frames from APOLLO_annotate_peaks(),
or a single data.frame. Each data.frame must have an 'annotation_simple'
or 'annotation' column.}

\item{use_simple}{Logical. If TRUE (default), uses 'annotation_simple' column.
If FALSE, uses full 'annotation' column.}

\item{show_percentage}{Logical. If TRUE (default), shows percentages.
If FALSE, shows raw counts.}

\item{colors}{Named character vector of colors for each annotation category.
If NULL, uses a default color scheme.}

\item{title}{Character. Plot title (default = "Genomic Feature Distribution").}

\item{bar_height}{Numeric. Height of each bar (default = 0.7).}

\item{show_labels}{Logical. If TRUE (default), shows percentage labels on bars.}

\item{min_label_pct}{Numeric. Minimum percentage to show label (default = 5).
Prevents cluttering small segments with labels.}
}
\value{
A ggplot object.
}
\description{
Creates a stacked horizontal bar plot showing the distribution
of genomic features (Promoter, Exon, Intron, Intergenic, etc.) for one or
more sets of annotated peaks. Useful for comparing annotation profiles
between different datasets (e.g., ATAC vs ChIP peaks).
}
\details{
The plot shows one horizontal stacked bar per dataset in annotated_list.
Each segment's width is proportional to the count/percentage of peaks
in that genomic category.

Default annotation categories (from annotation_simple):
\itemize{
\item Promoter: regions within TSS window
\item 5' UTR, 3' UTR: untranslated regions
\item Exon: exonic regions
\item Intron: intronic regions
\item Downstream: downstream of genes
\item Intergenic: between genes
}
}
\examples{
\dontrun{
# Single dataset
p <- AETHER_plot_annotation_bar(atac_annotated)

# Compare ATAC vs ChIP annotation profiles
annotated_list <- list(
  "ATAC peaks" = atac_annotated,
  "ChIP peaks" = chip_annotated
)
p <- AETHER_plot_annotation_bar(annotated_list)

# Custom colors
my_colors <- c(Promoter = "#e41a1c", Exon = "#377eb8", Intron = "#4daf4a")
p <- AETHER_plot_annotation_bar(annotated_list, colors = my_colors)

}
}
