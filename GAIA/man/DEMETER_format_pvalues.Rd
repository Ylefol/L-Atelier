% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demeter_correlation_utils.R
\name{DEMETER_format_pvalues}
\alias{DEMETER_format_pvalues}
\title{Format p-values for display}
\usage{
DEMETER_format_pvalues(
  pvalues,
  format = "scientific",
  digits = 2,
  sig_threshold = 0.05,
  hide_ns = FALSE,
  ns_string = ""
)
}
\arguments{
\item{pvalues}{Numeric vector or matrix of p-values.}

\item{format}{Output format: "scientific" (e.g., "1.23e-04"), "stars"
(*, **, ***), "decimal" (e.g., "0.001"), or "hybrid" (decimal if >= 0.001,
scientific otherwise). Default: "scientific".}

\item{digits}{Number of significant digits. Default: 2.}

\item{sig_threshold}{P-value threshold for significance. Values above this
become empty strings (if hide_ns = TRUE). Default: 0.05.}

\item{hide_ns}{Logical. Replace non-significant p-values with empty string.
Default: FALSE.}

\item{ns_string}{String to use for non-significant values when hide_ns = TRUE.
Default: "".}
}
\value{
Character vector or matrix of formatted p-values (same shape as input).
}
\description{
Converts p-values to formatted strings, optionally in scientific notation.
Non-significant values can be replaced with empty strings or NA.
}
\examples{
\dontrun{
pvals <- c(0.001, 0.023, 0.15, 0.0001)
DEMETER_format_pvalues(pvals)
DEMETER_format_pvalues(pvals, format = "stars")
DEMETER_format_pvalues(pvals, hide_ns = TRUE)

}
}
