% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_motif_plots.R
\name{AETHER_plot_motif_comparison}
\alias{AETHER_plot_motif_comparison}
\title{Heatmap of Motif Enrichment Across Peak Sets}
\usage{
AETHER_plot_motif_comparison(
  homer_batch,
  top_n = 20,
  motifs = NULL,
  cluster_sets = FALSE,
  cluster_motifs = TRUE,
  q_thresh = 0.05,
  colors = NULL,
  title = "Motif Enrichment Comparison",
  show_values = FALSE,
  ...
)
}
\arguments{
\item{homer_batch}{A homer_motif_batch object.}

\item{top_n}{Integer. Number of top motifs to include (default = 20).
Selected by best q-value across all sets.}

\item{motifs}{Character vector. Specific motif family names to include.
Overrides top_n if provided.}

\item{cluster_sets}{Logical. Hierarchically cluster columns (peak sets).
Default FALSE (preserves input order).}

\item{cluster_motifs}{Logical. Hierarchically cluster rows (motifs).
Default TRUE.}

\item{q_thresh}{Numeric. Significance threshold for motif selection
(default = 0.05). Motifs must be significant in at least one set.}

\item{colors}{Character vector of length 3. Low, mid, high colors for
the heatmap. Default: white to red.}

\item{title}{Character. Plot title (default = "Motif Enrichment Comparison").}

\item{show_values}{Logical. Show -log10(q) values in cells. Default FALSE.}

\item{...}{Additional arguments passed to pheatmap.}
}
\value{
A pheatmap object (invisibly).
}
\description{
Creates a heatmap showing -log10(q-value) for motifs across
multiple peak sets. Useful for comparing which TF motifs are enriched in
different conditions or clusters.
}
