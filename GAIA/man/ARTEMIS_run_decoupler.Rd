% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artemis_decoupler.R
\name{ARTEMIS_run_decoupler}
\alias{ARTEMIS_run_decoupler}
\title{Run decoupleR activity inference}
\usage{
ARTEMIS_run_decoupler(mat, network, method, minsize = 5, verbose = TRUE, ...)
}
\arguments{
\item{mat}{Numeric matrix of gene expression data. Genes as rows, samples as columns.
Rownames must be gene identifiers matching the network.}

\item{network}{Prior knowledge network (from APOLLO_get_* functions or custom).
Must have columns: 'source' (TF/pathway), 'target' (gene), and 'mor' or 'weight'.}

\item{method}{Character. Statistical method to use. REQUIRED - no default.
Options: "ulm", "mlm", "viper", "wsum", "wmean", "udt", "mdt", "ora", "gsva", "aucell".}

\item{minsize}{Integer. Minimum number of targets per source to include.
Default: 5.}

\item{verbose}{Logical. Print progress messages. Default: TRUE.}

\item{...}{Additional arguments passed to the specific method function.}
}
\value{
A list with class "decoupler_result" containing:
\item{activities}{Matrix of activity scores (sources x samples)}
\item{pvalues}{Matrix of p-values (if available for method)}
\item{results_long}{Full results in long format (from decoupleR)}
\item{method}{Method used}
\item{network_info}{Information about the network used}
\item{n_sources}{Number of sources (TFs/pathways) with results}
\item{n_samples}{Number of samples}
}
\description{
Core function for inferring biological activities (TF or pathway) from
gene expression data using a specified statistical method.
}
\details{
This function requires an explicit method choice to ensure users understand
which statistical approach they are using. For method recommendations, use
ARTEMIS_list_methods().

The input matrix should be normalized gene expression data (e.g., log-transformed
TPM or VST-normalized counts). Raw counts are not recommended.
}
\examples{
\dontrun{
# Get network
network <- APOLLO_get_collectri()

# Run ULM (recommended)
result <- ARTEMIS_run_decoupler(expr_matrix, network, method = "ulm")

# Run MLM
result <- ARTEMIS_run_decoupler(expr_matrix, network, method = "mlm")

}
}
