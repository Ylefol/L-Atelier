% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_differential_plots.R
\name{AETHER_plot_waterfall}
\alias{AETHER_plot_waterfall}
\title{Waterfall Plot of Fold Changes}
\usage{
AETHER_plot_waterfall(
  shift_result,
  n_label = 5,
  fc_threshold = 0.5,
  title = "Waterfall Plot of Fold Changes",
  colors = NULL
)
}
\arguments{
\item{shift_result}{Result from ARTEMIS_global_shift_test(), or a data.frame
with columns 'region_id' and 'log2FC'.}

\item{n_label}{Integer. Number of top/bottom regions to label (default = 5).
Set to 0 to disable labels.}

\item{fc_threshold}{Numeric. log2FC threshold for coloring bars as
"significant" (default = 0.5, i.e., ~1.4-fold). Bars beyond this threshold
are colored differently.}

\item{title}{Character. Plot title (default = "Waterfall Plot").}

\item{colors}{Named character vector with colors for "down", "neutral", "up"
(default uses blue/gray/red scheme).}
}
\value{
A ggplot object.
}
\description{
Creates a waterfall plot where regions are ranked by their
log2 fold change. Each bar represents one region, sorted from most negative
to most positive change. Useful for identifying outliers and seeing the
distribution of effects across regions.
}
\details{
The waterfall plot shows:
\itemize{
\item X-axis: regions ranked by log2FC (most decreased on left, most increased on right)
\item Y-axis: log2 fold change value
\item Bar color: blue for decreased (< -threshold), red for increased (> threshold),
gray for neutral
\item Labels: top N most extreme regions on each end
}

This visualization quickly answers:
\itemize{
\item How many regions go up vs down?
\item Are there outliers driving the effect?
\item Is the effect uniform or heterogeneous?
}
}
\examples{
\dontrun{
# From shift test result
p <- AETHER_plot_waterfall(shift_result)

# Label more regions
p <- AETHER_plot_waterfall(shift_result, n_label = 10)

# Stricter threshold for coloring
p <- AETHER_plot_waterfall(shift_result, fc_threshold = 1.0)

}
}
