% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eleuthia_discriminative_report.R
\name{ELEUTHIA_discriminative_report}
\alias{ELEUTHIA_discriminative_report}
\title{Generate Discriminative Analysis Report}
\usage{
ELEUTHIA_discriminative_report(
  glasso_fit,
  cv_result,
  selected,
  final_model = NULL,
  coef_table = NULL,
  encoded = NULL,
  output_dir = NULL,
  prefix = "discriminative",
  save_tables = TRUE,
  save_plots = TRUE,
  plot_width = 10,
  plot_height = 8,
  verbose = TRUE
)
}
\arguments{
\item{glasso_fit}{An artemis_group_lasso object from ARTEMIS_fit_group_lasso().}

\item{cv_result}{Output from ARTEMIS_select_lambda().}

\item{selected}{Output from ARTEMIS_extract_selected_variables().}

\item{final_model}{Optional. An artemis_final_model from ARTEMIS_fit_final_model().}

\item{coef_table}{Optional. Data.frame from ARTEMIS_extract_coefficients().}

\item{encoded}{Optional. Output from POSEIDON_encode_for_regression() for
additional context.}

\item{output_dir}{Character. Directory to save output files. If NULL (default),
only prints to console without saving files.}

\item{prefix}{Character. Prefix for output filenames. Default = "discriminative".}

\item{save_tables}{Logical. Save tables as CSV files. Default = TRUE.}

\item{save_plots}{Logical. Save plots as PNG files. Default = TRUE.}

\item{plot_width}{Numeric. Width of saved plots in inches. Default = 10.}

\item{plot_height}{Numeric. Height of saved plots in inches. Default = 8.}

\item{verbose}{Logical. Print report to console. Default = TRUE.}
}
\value{
Invisibly returns a list containing:
\describe{
\item{summary}{Character vector of the text report}
\item{tables}{List of data frames exported}
\item{files_saved}{Character vector of files saved}
}
}
\description{
Generate a comprehensive report from the discriminative variable
analysis pipeline, including variable selection results, model coefficients,
and optionally save tables and plots.
}
\details{
This function consolidates results from the discriminative analysis pipeline:
\itemize{
\item Group LASSO variable selection results
\item Cross-validation lambda selection
\item Selected variables and their effects
\item Final model coefficients (odds ratios or coefficients)
}

At minimum, glasso_fit, cv_result, and selected are required. Final model
and coefficients are optional but recommended for complete reporting.
}
\examples{
\dontrun{
# Console report only
ELEUTHIA_discriminative_report(glasso_fit, cv_result, selected)

# Full report with file export
ELEUTHIA_discriminative_report(
  glasso_fit = fit,
  cv_result = cv_result,
  selected = selected,
  final_model = final,
  coef_table = coefs,
  output_dir = "results/discriminative"
)

}
}
