% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_coverage_tracks.R
\name{AETHER_plot_coverage_tracks}
\alias{AETHER_plot_coverage_tracks}
\title{Plot Genome Coverage Tracks}
\usage{
AETHER_plot_coverage_tracks(
  bigwig_files = NULL,
  bed_files = NULL,
  region,
  chrom_sizes = NULL,
  normalize = "CPM",
  bin_size = 10L,
  peak_bed = NULL,
  annotation_tracks = NULL,
  group_colors = NULL,
  scale_y = "free",
  show_variance = FALSE,
  variance_type = "sd",
  track_height = 1,
  annotation_height = 0.2,
  peak_color = "steelblue4",
  vline = NULL,
  vline_color = "black",
  vline_type = "dotted",
  title = NULL,
  verbose = FALSE
)
}
\arguments{
\item{bigwig_files}{Track input. Two formats are accepted:
\itemize{
\item \strong{Flat} (current default): a named list or named character vector where
each element is a character vector of BigWig file paths for one track.
All tracks share a common y-axis when \code{scale_y = "fixed"}.
\item \strong{Grouped}: a named list of named lists/character vectors. Each outer
element defines a group of tracks (e.g. one omics type). When
\code{scale_y = "fixed"}, the y-axis is fixed \emph{within} each group but
allowed to differ across groups, so tracks of different dynamic range
(e.g. ATAC vs RNA) are not forced onto the same scale.
Example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  ATAC = list(ATAC_WT = c("wt1.bw","wt2.bw"), ATAC_KO = "ko.bw"),
  RNA  = list(RNA_WT  = "rna_wt.bw",           RNA_KO  = "rna_ko.bw")
)
}\if{html}{\out{</div>}}
}

Mutually exclusive with \code{bed_files}.}

\item{bed_files}{Named list of character vectors, one element per track.
Each element is a character vector of fragment-level BED file paths.
Coverage is computed on-the-fly for the requested region only.
A named character vector is accepted as shorthand. Mutually exclusive
with \code{bigwig_files}. Requires \code{chrom_sizes}.}

\item{region}{Genomic region to display. One of:
\itemize{
\item Character string: \code{"chr1:1000000-2000000"}
\item Named character vector: \code{c(chr="chr1", start="1000000", end="2000000")}
\item A \code{GRanges} object (single range)
}}

\item{chrom_sizes}{Required when using \code{bed_files}. Exact chromosome
lengths as a named numeric vector or \code{APOLLO_get_chromosome_sizes()}
data.frame. Ignored in BigWig mode.}

\item{normalize}{Character. Normalization for BED mode only: \code{"CPM"}
(default) or \code{"raw"}. Ignored in BigWig mode (signal is already
normalized in the file).}

\item{bin_size}{Integer. Bin width in bp for BED mode only. Default \code{10}.
Ignored in BigWig mode.}

\item{peak_bed}{Optional. Peak annotation track displayed below coverage
tracks. Either a path to a BED file or a data.frame with at least three
columns (chr, start, end). Equivalent to adding a \code{"region"} type entry
to \code{annotation_tracks} named \code{"Peaks"}.}

\item{annotation_tracks}{Optional named list of additional annotation tracks
to display below coverage tracks (and below \code{peak_bed} if provided). Each
element is a named list describing one track. Two types are supported:
\describe{
\item{\code{type = "region"}}{Genomic intervals shown as filled rectangles
(e.g. CpG islands, repeats). Required field: \code{data} (BED file path or
data.frame with chr/start/end columns). Optional fields: \code{color}
(fill colour, default \code{"steelblue4"}), \code{height} (relative patchwork
height, default = \code{annotation_height}).}
\item{\code{type = "score"}}{Per-site scores shown as a lollipop plot — a
vertical segment from 0 to the score, capped with a point, both
coloured by score value (e.g. methylation fractions). Required field:
\code{data} (BigWig file path). Optional fields: \code{color_low} (colour at
score=0, default \code{"steelblue"}), \code{color_high} (colour at score=1,
default \code{"firebrick"}), \code{score_limits} (numeric(2), y-axis and colour
scale limits; auto-detected from data if \code{NULL}), \code{height} (relative
patchwork height, default = \code{annotation_height * 2}).}
\item{\code{type = "genes"}}{Collapsed IGV-style gene model track. Shows
the intron backbone, exon blocks (UTR height), CDS blocks (taller),
strand direction arrows, and italic gene name labels. Multiple genes
are automatically stacked into rows to avoid overlap. Required field:
either \code{txdb} (TxDb object from \code{APOLLO_make_txdb()} — fast, preferred)
or \code{gtf_file} (path to GTF — slower, reads genome-wide). Optional
fields: \code{chr_mapping} (e.g. \code{"T2T"}, applied when using \code{gtf_file}),
\code{show_cds} (logical, default \code{TRUE}), \code{label_genes} (logical, default
\code{TRUE}), \code{label_size} (default \code{2.5}), \code{color} (default \code{"black"}),
\code{height} (default = \code{annotation_height * 3}).}
}
Example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{annotation_tracks = list(
  "CpG Islands" = list(type="region", data="cpg.bed", color="darkgreen"),
  "GpC Meth"    = list(type="score",  data="meth.bw",
                       color_low="steelblue", color_high="firebrick")
)
}\if{html}{\out{</div>}}}

\item{group_colors}{Optional named character vector mapping track names to
colors. If \code{NULL}, colors are assigned automatically — known group names
(WT, KO, etc.) use the AETHER default palette; others are generated.}

\item{scale_y}{Character. Y-axis scaling: \code{"free"} (default, each track
autoscales independently) or \code{"fixed"} (tracks share the same y-axis
maximum — globally when \code{bigwig_files} is flat, or within each group when
\code{bigwig_files} is grouped).}

\item{show_variance}{Logical. When a track contains multiple files, display
a shaded ribbon showing the spread across replicates. Default \code{FALSE}.
Has no effect on single-file tracks.}

\item{variance_type}{Character. Type of variance ribbon when
\code{show_variance = TRUE}: \code{"sd"} (default, mean ± 1 SD) or \code{"range"}
(min to max across replicates).}

\item{track_height}{Numeric. Relative height unit for each coverage track.
Default \code{1}.}

\item{annotation_height}{Numeric. Default relative height unit for
annotation tracks (\code{peak_bed} and \code{annotation_tracks} entries that do not
specify their own \code{height}). Default \code{0.2}.}

\item{peak_color}{Character. Fill color for peak rectangles in the
\code{peak_bed} annotation track. Default \code{"steelblue4"}.}

\item{vline}{Optional numeric vector of genomic positions (bp) at which to
draw a vertical reference line through every track and annotation panel.
Each position is drawn as an independent line within its panel, so panels
are not visually connected. Accepts either plain numerics or
\code{"chr:position"} strings (e.g. \code{"chr10:74073596"}) — the chromosome is
ignored since all panels share the same region. Default \code{NULL} (no lines).}

\item{vline_color}{Character. Colour of the vertical reference line(s).
Default \code{"black"}.}

\item{vline_type}{Character. Line type of the vertical reference line(s)
(any value accepted by \code{linetype}). Default \code{"dotted"}.}

\item{title}{Optional character string. Plot title placed above the first
track.}

\item{verbose}{Logical. Print progress messages. Default \code{FALSE}.}
}
\value{
A \code{patchwork} plot object (combination of \code{ggplot} panels).
}
\description{
Produces a genome browser-style stacked coverage track plot
over a specified genomic region. Each named track shows the read pileup
signal as a filled area curve. Multiple files per track are averaged into
a single signal; an optional variance ribbon can be displayed when
replicates are provided.

Two input modes are supported — provide exactly one:
\itemize{
\item \strong{BigWig mode} (\code{bigwig_files}): reads signal from pre-computed BigWig
files. Use when BigWigs already exist (e.g. from \code{\link[=ELEUTHIA_bed_to_bigwig]{ELEUTHIA_bed_to_bigwig()}}).
\item \strong{BED mode} (\code{bed_files}): computes coverage on-the-fly for the
requested region directly from fragment-level BED files via
\code{ELEUTHIA_bed_region_coverage()}. More efficient when only a small region
is needed and BigWig files have not been pre-generated.
}

An optional peak annotation track can be added at the bottom. Additional
annotation tracks (e.g. CpG islands, methylation sites) can be added via
\code{annotation_tracks}.
}
\details{
When multiple files are provided for a track, per-bin mean is computed
across all files. Bins absent in a file are treated as zero before
averaging.

BigWig mode requires: \code{GenomicRanges}, \code{IRanges}, \code{rtracklayer}.
BED mode requires: \code{data.table}, \code{GenomicRanges}, \code{IRanges}, \code{GenomeInfoDb}.
Both modes require: \code{patchwork}.
}
