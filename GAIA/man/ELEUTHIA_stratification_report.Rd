% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eleuthia_stratification_report.R
\name{ELEUTHIA_stratification_report}
\alias{ELEUTHIA_stratification_report}
\title{Generate Stratification Pipeline Report}
\usage{
ELEUTHIA_stratification_report(
  cluster_result,
  stability_result = NULL,
  char_result = NULL,
  famd_result = NULL,
  comparison_result = NULL,
  qc_result = NULL,
  output_dir = NULL,
  prefix = "stratification",
  save_tables = TRUE,
  save_plots = TRUE,
  plot_width = 10,
  plot_height = 8,
  verbose = TRUE
)
}
\arguments{
\item{cluster_result}{An artemis_cluster object from ARTEMIS_cluster_mixed().}

\item{stability_result}{Optional. An artemis_stability object from
ARTEMIS_cluster_stability().}

\item{char_result}{Optional. An artemis_characterization object from
ARTEMIS_characterize_clusters().}

\item{famd_result}{Optional. An artemis_famd object from ARTEMIS_famd().}

\item{comparison_result}{Optional. An artemis_variable_comparison object
from ARTEMIS_compare_variable_importance().}

\item{qc_result}{Optional. A hades_qc object from HADES_qc_mixed_data().}

\item{output_dir}{Character. Directory to save output files. If NULL (default),
only prints to console without saving files.}

\item{prefix}{Character. Prefix for output filenames. Default = "stratification".}

\item{save_tables}{Logical. Save key tables as CSV files. Default = TRUE
(when output_dir is provided).}

\item{save_plots}{Logical. Save plots as PNG files. Default = TRUE
(when output_dir is provided).}

\item{plot_width}{Numeric. Width of saved plots in inches. Default = 10.}

\item{plot_height}{Numeric. Height of saved plots in inches. Default = 8.}

\item{verbose}{Logical. Print report to console. Default = TRUE.}
}
\value{
Invisibly returns a list containing:
\describe{
\item{summary}{Character vector of the text report}
\item{tables}{List of data frames that were/would be exported}
\item{files_saved}{Character vector of files saved (if output_dir provided)}
}
}
\description{
Generate a comprehensive report from the patient stratification
pipeline, including clustering results, stability assessment, variable
importance, and optionally save tables and plots.
}
\details{
This function consolidates results from the stratification pipeline into
a single report. It can:
\itemize{
\item Print a formatted summary to console
\item Save the summary as a text file
\item Export key tables (cluster assignments, variable importance, etc.) as CSV
\item Generate and save standard plots (silhouette, FAMD, radar)
}

At minimum, cluster_result is required. Other results are optional and
will be included if provided.
}
\examples{
\dontrun{
# Generate console report only
ELEUTHIA_stratification_report(cluster_result)

# Full report with file export
ELEUTHIA_stratification_report(
  cluster_result = clust,
  stability_result = stab,
  char_result = char,
  famd_result = famd,
  output_dir = "results/stratification"
)

}
}
