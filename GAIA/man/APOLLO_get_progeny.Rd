% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apollo_activity_networks.R
\name{APOLLO_get_progeny}
\alias{APOLLO_get_progeny}
\title{Get PROGENy pathway signatures}
\usage{
APOLLO_get_progeny(organism = "human", top = 500, verbose = TRUE, ...)
}
\arguments{
\item{organism}{Character. Organism to use: "human" or "mouse". Default: "human".}

\item{top}{Integer. Number of top responsive genes per pathway to include.
Default: 500. Use higher values for more coverage, lower for more specificity.}

\item{verbose}{Logical. Print information about the network. Default: TRUE.}

\item{...}{Additional arguments passed to decoupleR::get_progeny().}
}
\value{
A tibble with columns:
\item{source}{Pathway name}
\item{target}{Responsive gene name}
\item{weight}{Gene weight/importance for pathway activity}
}
\description{
Retrieves PROGENy pathway-responsive gene signatures. PROGENy provides
genes that are responsive to pathway activity, derived from perturbation
experiments.
}
\details{
PROGENy pathways include:
\itemize{
\item Androgen, EGFR, Estrogen, Hypoxia, JAK-STAT, MAPK, NFkB, p53, PI3K,
TGFb, TNFa, Trail, VEGF, WNT
}

The \code{top} parameter controls how many genes per pathway are included.
More genes = better coverage but potentially more noise.
Fewer genes = more specific but may miss relevant signals.
}
\examples{
\dontrun{
# Get PROGENy signatures (default 500 genes per pathway)
network <- APOLLO_get_progeny()

# Get more specific signatures (100 genes per pathway)
network_specific <- APOLLO_get_progeny(top = 100)

}
}
