% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hades_massspec.R
\name{HADES_detect_outliers_massspec}
\alias{HADES_detect_outliers_massspec}
\title{Detect Sample Outliers in Mass Spectrometry Data}
\usage{
HADES_detect_outliers_massspec(
  massspec_data,
  n_pcs = 5L,
  threshold = 3,
  method = c("sd", "mad"),
  filter = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{massspec_data}{A \code{massspec_data} object after filtering and
normalization.}

\item{n_pcs}{Integer. Number of principal components for the distance
computation. Default: 5.}

\item{threshold}{Numeric. Multiplier applied to the spread measure above the
centre. Default: 3.}

\item{method}{Character. \code{"sd"} (mean + threshold × sd) or
\code{"mad"} (median + threshold × mad). Default: \code{"sd"}.}

\item{filter}{Logical. If TRUE, flagged samples are removed from
\code{$wide} and \code{$sample_meta}. Default: FALSE (annotate only).}

\item{verbose}{Logical. Print detection summary. Default: TRUE.}
}
\value{
A \code{massspec_data} object. \code{$sample_meta} gains
\code{outlier_distance} and \code{outlier} columns.
}
\description{
Flags multivariate outliers in a \code{massspec_data} object
using PCA-based Euclidean distance from the sample centroid. Mean imputation
and per-protein scaling are applied internally for the outlier computation
only — log2 values in \code{$wide} are never modified.
}
\examples{
\dontrun{
ms <- HADES_detect_outliers_massspec(ms, filter = FALSE)
ms$sample_meta[ms$sample_meta$outlier, c("SampleID", "outlier_distance")]
}
}
