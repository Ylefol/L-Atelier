% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apollo_activity_networks.R
\name{.apollo_string_api_post}
\alias{.apollo_string_api_post}
\title{POST a form-encoded request to the STRING API}
\usage{
.apollo_string_api_post(path, params, api_base = "https://string-db.org/api")
}
\arguments{
\item{path}{Character. API path after \code{api_base/}, e.g.
\code{"tsv/network"} or \code{"image/network"}.}

\item{params}{Named list of POST body fields (character/numeric scalars).}

\item{api_base}{Character. STRING API base URL.}
}
\value{
The raw response list from \code{curl::curl_fetch_memory()}
(\code{$status_code}, \code{$content}, ...).
}
\description{
Sends \code{params} as an \code{application/x-www-form-urlencoded}
POST body (the same key=value pairs STRING's GET endpoints accept as query
parameters, just relocated to the request body). Used by
\code{\link{APOLLO_get_string_ppi}} instead of GET so identifier lists in
the hundreds/thousands don't hit the URL-length ceiling GET requests run
into. Values already containing percent-encoded characters (e.g. the
\code{identifiers} field, joined with \code{\%0d}) are passed through as-is.
}
\keyword{internal}
