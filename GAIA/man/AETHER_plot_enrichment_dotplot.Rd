% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aether_enrichment_plots.R
\name{AETHER_plot_enrichment_dotplot}
\alias{AETHER_plot_enrichment_dotplot}
\title{Dotplot for Enrichment Results}
\usage{
AETHER_plot_enrichment_dotplot(
  enrich_result,
  show_category = 20,
  color_by = "p.adjust",
  size_by = "Count",
  order_by = "p.adjust",
  title = "Enrichment Analysis",
  x_label = NULL,
  font_size = 10,
  color_low = "#2166ac",
  color_high = "#b2182b",
  color_midpoint = NULL
)
}
\arguments{
\item{enrich_result}{An enrichResult object from clusterProfiler (output of
APOLLO_enrich_go or APOLLO_enrich_kegg), or a data.frame with the required
columns.}

\item{show_category}{Integer. Number of top categories to display (default = 20).}

\item{color_by}{Character. Variable for color scale: "p.adjust" (default),
"pvalue", or "odds_ratio".}

\item{size_by}{Character. Variable for dot size: "Count" (default) or
"GeneRatio".}

\item{order_by}{Character. Variable to order terms: "p.adjust" (default),
"pvalue", "odds_ratio", or "Count".}

\item{title}{Character. Plot title (default = "Enrichment Analysis").}

\item{x_label}{Character. X-axis label. If NULL, auto-generated based on
size_by parameter.}

\item{font_size}{Numeric. Base font size for term labels (default = 10).}

\item{color_low}{Character. Color for low values (default = "#2166ac" blue).}

\item{color_high}{Character. Color for high values (default = "#b2182b" red).}

\item{color_midpoint}{Numeric or NULL. Midpoint for diverging color scale.
If NULL, uses sequential scale.}
}
\value{
A ggplot object.
}
\description{
Creates a dotplot visualization for GO/KEGG enrichment results.
Supports coloring by p-value, adjusted p-value, or odds ratio.
}
\details{
The dotplot shows:
\itemize{
\item Y-axis: enriched terms (GO terms or KEGG pathways)
\item X-axis: gene ratio or count
\item Dot size: number of genes (or gene ratio)
\item Dot color: significance (p-value/q-value) or effect size (odds ratio)
}

\strong{Why use Odds Ratio for color?}
P-values conflate effect size with sample size. A pathway with OR=10 and
p=0.01 may be more biologically relevant than OR=1.5 with p=1e-10.
Odds ratio directly measures the strength of association:
\itemize{
\item OR = 1: no association
\item OR > 1: enrichment (higher = stronger)
\item OR < 1: depletion
}
}
\examples{
\dontrun{
# Standard dotplot (color by adjusted p-value)
p <- AETHER_plot_enrichment_dotplot(go_results)

# Color by odds ratio for effect size
p <- AETHER_plot_enrichment_dotplot(go_results, color_by = "odds_ratio")

# Show more categories, order by odds ratio
p <- AETHER_plot_enrichment_dotplot(
  go_results,
  show_category = 30,
  color_by = "odds_ratio",
  order_by = "odds_ratio"
)

}
}
