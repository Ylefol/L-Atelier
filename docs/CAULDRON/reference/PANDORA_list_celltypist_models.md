# List available CellTypist models

Fetches the CellTypist model registry and prints a summary of all
available models. Requires an internet connection (model index is hosted
by the Sanger Institute).

## Usage

``` r
PANDORA_list_celltypist_models()
```

## Value

Invisibly returns a `data.frame` of model information.
