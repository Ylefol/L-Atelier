# Concatenate files at the byte level internal

Concatenate files at the byte level internal

## Usage

``` r
.horizon_concat_bytes(input_files, output_file)
```

## Arguments

- input_files:

  Character vector of input file paths, written to `output_file` in
  order.

- output_file:

  Character. Destination file path.
