# Stop Console Logging

Stops stdout duplication and appends session information.

## Usage

``` r
HORIZON_stop_log()
```

## Value

Invisibly returns the log file path, or NULL if no log was active.
