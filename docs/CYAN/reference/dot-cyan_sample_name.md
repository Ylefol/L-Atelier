# Derive a sample name from a BAM file path Strip directory and extension, use as a safe file-system label.

Derive a sample name from a BAM file path Strip directory and extension,
use as a safe file-system label.

## Usage

``` r
.cyan_sample_name(bam, sample_name = NULL)
```
