% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aspis_markers.R
\name{ASPIS_plot_db_heatmap}
\alias{ASPIS_plot_db_heatmap}
\title{Marker database heatmap}
\usage{
ASPIS_plot_db_heatmap(
  sce,
  markers,
  cluster_col = "cluster",
  assay_name = "logcounts",
  scale = TRUE,
  na_color = "grey80",
  palette = NULL,
  show_gene_labels = TRUE,
  title = "Marker database heatmap"
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with a \code{logcounts} (or other)
assay.}

\item{markers}{A \code{data.frame} with columns:
\describe{
\item{cell_type}{Cell type name used for row faceting.}
\item{markers}{Positive marker genes, comma-separated string.}
\item{neg_markers}{(Optional) Negative marker genes, comma-separated.
When present, positive and negative genes are shown in nested
sub-facets.}
}}

\item{cluster_col}{Character. \code{colData} column containing cluster
labels used for column grouping.  Default \code{"cluster"}.}

\item{assay_name}{Character. Assay to pull expression from.
Default \code{"logcounts"}.}

\item{scale}{Logical. Z-score mean expression per gene across clusters.
Default \code{TRUE}.}

\item{na_color}{Character. Fill colour for genes absent from the SCE.
Default \code{"grey80"}.}

\item{palette}{Character vector of colours for the fill gradient.
\code{NULL} (default) uses a blue-white-red diverging palette.}

\item{show_gene_labels}{Logical. Show gene names on the y axis.
Default \code{TRUE}.}

\item{title}{Character. Plot title.  Default \code{"Marker database heatmap"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualises cluster-averaged expression of genes drawn from a user-supplied
marker database.  Rows are genes grouped into facets by cell type; columns
are clusters.  Genes absent from the SCE are retained as grey \code{NA}
tiles so coverage gaps are immediately visible.  When a \code{neg_markers}
column is present, positive and negative markers form nested sub-facets
within each cell type.
}
