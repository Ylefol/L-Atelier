% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aspis_trajectory.R
\name{ASPIS_plot_velocity}
\alias{ASPIS_plot_velocity}
\title{Plot RNA velocity with coloured per-cell arrows and origin circles}
\usage{
ASPIS_plot_velocity(
  sce,
  dimred = "UMAP",
  colour_by = "cluster",
  arrow_scale = 1,
  arrow_density = 0.3,
  arrow_alpha = 0.9,
  arrow_size = 0.4,
  arrow_length = 0.08,
  circle_size = 3,
  circle_alpha = 0.2,
  point_size = 0.5,
  point_alpha = 0.8,
  palette = NULL,
  assay_name = "logcounts",
  title = NULL
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with velocity results from
\code{\link{TRIPODES_run_velocity}}.}

\item{dimred}{Character. Embedding to use. Default \code{"UMAP"}.}

\item{colour_by}{Character. \code{colData} column or gene name for cell
colour. Default \code{"cluster"}.}

\item{arrow_scale}{Numeric. Multiplier on arrow length. Default \code{1}.}

\item{arrow_density}{Numeric in (0,1]. Fraction of cells that get arrows.
Reduce for large datasets. Default \code{0.3}.}

\item{arrow_alpha}{Numeric. Arrow opacity. Default \code{0.9}.}

\item{arrow_size}{Numeric. Arrow line width. Default \code{0.4}.}

\item{arrow_length}{Numeric. Arrowhead length in cm. Default \code{0.08}.}

\item{circle_size}{Numeric. Size of the semi-transparent origin circle.
Default \code{3}.}

\item{circle_alpha}{Numeric. Opacity of the origin circle. Default \code{0.2}.}

\item{point_size}{Numeric. Cell point size. Default \code{0.5}.}

\item{point_alpha}{Numeric. Cell point opacity. Default \code{0.8}.}

\item{palette}{Character vector or \code{NULL} for defaults.}

\item{assay_name}{Character. Assay for gene expression. Default
\code{"logcounts"}.}

\item{title}{Character or \code{NULL}. Plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Overlays per-cell velocity arrows on a UMAP or tSNE embedding.  Each cell's
arrow and its semi-transparent origin circle share the cell's
cluster/feature colour, replicating the \code{scv.pl.velocity_embedding}
style.  For smooth streamlines see \code{\link{ASPIS_plot_velocity_stream}}.
}
