% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aspis_markers.R
\name{ASPIS_plot_marker_heatmap}
\alias{ASPIS_plot_marker_heatmap}
\title{Marker gene heatmap}
\usage{
ASPIS_plot_marker_heatmap(
  sce,
  markers,
  cluster_col = "cluster",
  assay_name = "logcounts",
  top_n = 5L,
  scale = TRUE,
  palette = NULL,
  show_gene_labels = TRUE,
  title = "Marker gene heatmap"
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with a \code{logcounts} (or
other) assay.}

\item{markers}{A \code{keraunos_markers} object returned by
\code{\link{KERAUNOS_find_markers}}, or a character vector of gene names.}

\item{cluster_col}{Character. \code{colData} column containing cluster
labels.  Default \code{"cluster"}.}

\item{assay_name}{Character. Assay to pull expression from.
Default \code{"logcounts"}.}

\item{top_n}{Integer. Maximum marker genes per cluster shown when
\code{markers} is a \code{keraunos_markers} object.  Default \code{5L}.}

\item{scale}{Logical. Z-score mean expression per gene across clusters.
Default \code{TRUE}.}

\item{palette}{Character vector of colours for the fill gradient.
\code{NULL} (default) uses a blue–white–red diverging palette.}

\item{show_gene_labels}{Logical. Show gene names on the y axis.
Default \code{TRUE}.}

\item{title}{Character. Plot title.  Default \code{"Marker gene heatmap"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualises cluster-averaged expression of marker genes as a tiled heatmap.
When \code{markers} is a \code{keraunos_markers} object, genes are grouped
into facets by their source cluster so marker identity is immediately clear.
Columns are clusters; rows are genes; colour encodes z-scored (or raw) mean
expression.
}
