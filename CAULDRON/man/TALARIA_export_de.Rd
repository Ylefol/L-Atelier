% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/talaria_export.R
\name{TALARIA_export_de}
\alias{TALARIA_export_de}
\title{Export DE results to files}
\usage{
TALARIA_export_de(
  de_result,
  output_dir,
  prefix = "de",
  plots = TRUE,
  top_n = 10L,
  l2fc_thresh = 1,
  plot_width = 8,
  plot_height = 6,
  verbose = TRUE
)
}
\arguments{
\item{de_result}{A \code{keraunos_markers}, \code{keraunos_contrast}, or
\code{keraunos_pseudobulk} object.}

\item{output_dir}{Character. Directory to write into (created if absent).}

\item{prefix}{Character. Filename prefix. Default \code{"de"}.}

\item{plots}{Logical. Generate volcano and MA plots (pseudobulk only).
Default \code{TRUE}.}

\item{top_n}{Integer. Number of genes to label on each plot by significance.
Requires \code{ggrepel} (Suggests). Default \code{10L}.}

\item{l2fc_thresh}{Numeric. \eqn{\log_2} fold-change threshold for
up/down categories on plots. Default \code{1}.}

\item{plot_width}{Numeric. PNG width in inches. Default \code{8}.}

\item{plot_height}{Numeric. PNG height in inches. Default \code{6}.}

\item{verbose}{Logical. Print a summary. Default \code{TRUE}.}
}
\value{
Invisibly, a character vector of file paths written.
}
\description{
Writes CSV tables, an RDS object, and optional plots for a
\code{keraunos_markers}, \code{keraunos_contrast}, or
\code{keraunos_pseudobulk} object.
}
\details{
Volcano and MA plots are generated for \code{keraunos_pseudobulk} only
(requires \code{log2FoldChange}, \code{pvalue}, and \code{baseMean} columns
from DESeq2).  Marker and contrast results receive only CSV + RDS.
}
