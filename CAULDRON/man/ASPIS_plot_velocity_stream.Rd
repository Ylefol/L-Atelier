% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aspis_trajectory.R
\name{ASPIS_plot_velocity_stream}
\alias{ASPIS_plot_velocity_stream}
\title{Plot RNA velocity as smooth streamlines over a KDE cluster background}
\usage{
ASPIS_plot_velocity_stream(
  sce,
  dimred = "UMAP",
  colour_by = "cluster",
  grid_res = 30,
  n_streams = 200,
  stream_length = 50,
  stream_colour = "black",
  stream_size = 0.4,
  stream_alpha = 0.8,
  bandwidth = NULL,
  blob_alpha = 0.25,
  blob_bins = 4,
  show_points = FALSE,
  point_size = 0.3,
  point_alpha = 0.3,
  min_vel_frac = 0.05,
  palette = NULL,
  assay_name = "logcounts",
  title = NULL
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with velocity results from
\code{\link{TRIPODES_run_velocity}}.}

\item{dimred}{Character. Embedding to use. Default \code{"UMAP"}.}

\item{colour_by}{Character. \code{colData} column used for blob and
(optionally) streamline colour.  Must be discrete. Default \code{"cluster"}.}

\item{grid_res}{Integer. Resolution of the velocity grid (grid_res × grid_res
cells). Higher values give smoother fields but are slower to compute.
Default \code{30}.}

\item{n_streams}{Integer. Number of streamline seed points. Default
\code{200}.}

\item{stream_length}{Integer. Maximum integration steps per streamline.
Default \code{50}.}

\item{stream_colour}{Character colour string, or \code{NULL}.  A fixed string
(e.g. \code{"black"}) renders all streamlines in that colour.  \code{NULL}
colours each streamline by the nearest cell's \code{colour_by} value,
matching the blob palette. Default \code{"black"}.}

\item{stream_size}{Numeric. Streamline line width. Default \code{0.4}.}

\item{stream_alpha}{Numeric. Streamline opacity. Default \code{0.8}.}

\item{bandwidth}{Numeric or \code{NULL}. Gaussian kernel bandwidth for
velocity field interpolation.  \code{NULL} auto-scales to grid spacing.}

\item{blob_alpha}{Numeric. Maximum opacity of KDE polygon fills. Default
\code{0.25}.}

\item{blob_bins}{Integer. Number of density contour levels per cluster.
Fewer bins produce softer blobs. Default \code{4}.}

\item{show_points}{Logical. Whether to overlay individual cell points.
Default \code{FALSE}.}

\item{point_size}{Numeric. Cell point size (if \code{show_points=TRUE}).
Default \code{0.3}.}

\item{point_alpha}{Numeric. Cell point opacity. Default \code{0.3}.}

\item{min_vel_frac}{Numeric. Fraction of mean velocity magnitude below which
a streamline step is considered stalled and tracing stops. Default
\code{0.05}.}

\item{palette}{Character vector or \code{NULL} for defaults.}

\item{assay_name}{Character. Assay for gene expression. Default
\code{"logcounts"}.}

\item{title}{Character or \code{NULL}. Plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Replicates the \code{scv.pl.velocity_embedding_stream} style.  Per-cluster
KDE density blobs provide the colour context; streamlines show the inferred
directional flow of RNA velocity across the embedding.
}
\details{
Streamlines are computed by interpolating per-cell velocity vectors onto a
regular grid (Gaussian kernel weighting) and then integrating forward along
unit-normalised direction vectors using Euler steps.
}
