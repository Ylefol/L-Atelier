% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/talos_embedding.R
\name{TALOS_run_tsne}
\alias{TALOS_run_tsne}
\title{tSNE embedding}
\usage{
TALOS_run_tsne(
  sce,
  n_pcs = 30L,
  perplexity = 30,
  max_iter = 1000L,
  seed = 42L,
  verbose = TRUE
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with a \code{"PCA"} reduced
dimension.}

\item{n_pcs}{Integer. Number of PCA components to use. Default \code{30}.}

\item{perplexity}{Numeric. tSNE perplexity parameter — roughly the number
of effective nearest neighbours.  Typical range 5–50. Default \code{30}.}

\item{max_iter}{Integer. Maximum number of tSNE iterations. Default
\code{1000}.}

\item{seed}{Integer. Random seed. Default \code{42L}.}

\item{verbose}{Logical. Print a summary. Default \code{TRUE}.}
}
\value{
SCE with \code{reducedDims(sce)[["TSNE"]]} populated.
}
\description{
Computes a 2-dimensional tSNE embedding from the PCA reduced dimensions.
Requires \code{\link{TALOS_run_pca}} to have been run first.  Results are
stored in \code{reducedDims(sce)[["TSNE"]]}.
}
