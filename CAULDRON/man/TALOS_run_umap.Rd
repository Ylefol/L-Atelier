% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/talos_embedding.R
\name{TALOS_run_umap}
\alias{TALOS_run_umap}
\title{UMAP embedding}
\usage{
TALOS_run_umap(
  sce,
  n_pcs = 30L,
  n_neighbors = 15L,
  min_dist = 0.1,
  seed = 42L,
  verbose = TRUE
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with a \code{"PCA"} reduced
dimension.}

\item{n_pcs}{Integer. Number of PCA components to use as input.
Default \code{30}.}

\item{n_neighbors}{Integer. Number of nearest neighbours for the UMAP graph.
Higher values produce a more global view; lower values emphasise local
structure. Default \code{15}.}

\item{min_dist}{Numeric. Minimum distance between points in the embedding.
Smaller values pack points more tightly. Default \code{0.1}.}

\item{seed}{Integer. Random seed for reproducibility. Default \code{42L}.}

\item{verbose}{Logical. Print a summary. Default \code{TRUE}.}
}
\value{
SCE with \code{reducedDims(sce)[["UMAP"]]} populated.
}
\description{
Computes a 2-dimensional UMAP embedding from the PCA reduced dimensions.
Requires \code{\link{TALOS_run_pca}} to have been run first.  Results are
stored in \code{reducedDims(sce)[["UMAP"]]}.
}
