% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/talaria_export.R
\name{TALARIA_export_ora}
\alias{TALARIA_export_ora}
\title{Export ORA results to files}
\usage{
TALARIA_export_ora(
  result,
  output_dir,
  prefix = "ora",
  plots = TRUE,
  top_n = 20L,
  plot_width = 8,
  plot_height = 7,
  verbose = TRUE
)
}
\arguments{
\item{result}{A \code{keraunos_ora} or \code{keraunos_ora_multi} object.}

\item{output_dir}{Character. Directory to write into (created if absent).}

\item{prefix}{Character. Filename prefix. Default \code{"ora"}.}

\item{plots}{Logical. Generate dotplots. Default \code{TRUE}.}

\item{top_n}{Integer. Total terms shown per dotplot, distributed across
sources. Default \code{20L}.}

\item{plot_width}{Numeric. PNG width in inches. Default \code{8}.}

\item{plot_height}{Numeric. PNG height in inches. Default \code{7}.}

\item{verbose}{Logical. Print a summary. Default \code{TRUE}.}
}
\value{
Invisibly, a character vector of file paths written.
}
\description{
Writes CSV tables, an RDS object, and optional fold-enrichment dotplots for
a \code{keraunos_ora} or \code{keraunos_ora_multi} object.
}
\details{
For \code{keraunos_ora_multi} each cluster gets its own subdirectory.
Up- and down-regulated directions are exported separately.
}
