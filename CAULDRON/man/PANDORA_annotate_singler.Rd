% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pandora_annotation.R
\name{PANDORA_annotate_singler}
\alias{PANDORA_annotate_singler}
\title{Reference-based cell type annotation via SingleR}
\usage{
PANDORA_annotate_singler(
  sce,
  reference,
  labels_col = "label.main",
  cluster_col = NULL,
  assay_name = "logcounts",
  label_col = "singler_label",
  prune = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with a \code{"logcounts"} (or
other) assay.}

\item{reference}{A \code{SummarizedExperiment} reference, or a character
shorthand: one of \code{"HumanPrimaryCellAtlas"},
\code{"BlueprintEncode"}, \code{"DICE"}, \code{"Monaco"},
\code{"Novershtern"} (human), or \code{"ImmGen"},
\code{"MouseRNAseq"} (mouse).}

\item{labels_col}{Character. Column in the reference \code{colData} to use
as labels.  Default \code{"label.main"}.  Use \code{"label.fine"} for
finer resolution.}

\item{cluster_col}{Character or \code{NULL}.  If \code{NULL} (default),
annotation is at the cell level.  If a \code{colData} column name is
provided, expression is aggregated per cluster before annotation —
one label per cluster is broadcast back to all cells in that cluster.}

\item{assay_name}{Character. Assay to use. Default \code{"logcounts"}.}

\item{label_col}{Character. \code{colData} column for the result.
Default \code{"singler_label"}.}

\item{prune}{Logical. Replace low-confidence labels with \code{NA} using
SingleR's delta-score pruning.  Default \code{TRUE}.}

\item{verbose}{Logical. Print a summary. Default \code{TRUE}.}
}
\value{
SCE with \code{colData(sce)[[label_col]]} populated.
}
\description{
Annotates cells (or clusters) by correlating expression profiles against a
reference dataset using \code{SingleR::SingleR()}.  A pre-loaded
\code{SummarizedExperiment} reference or a shorthand string for a
\code{celldex} dataset can be supplied.  Run
\code{\link{PANDORA_list_references}} to see available shorthand values.
}
