% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aspis_markers.R
\name{ASPIS_plot_marker_dotplot}
\alias{ASPIS_plot_marker_dotplot}
\title{Marker gene dot plot}
\usage{
ASPIS_plot_marker_dotplot(
  sce,
  markers,
  cluster_col = "cluster",
  assay_name = "logcounts",
  top_n = 5L,
  scale = TRUE,
  min_expr = 0,
  dot_max_size = 6,
  palette = NULL,
  title = "Marker genes"
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with a \code{logcounts} (or
other) assay.}

\item{markers}{A \code{keraunos_markers} object returned by
\code{\link{KERAUNOS_find_markers}}, or a character vector of gene names.}

\item{cluster_col}{Character. \code{colData} column containing cluster
labels.  Default \code{"cluster"}.}

\item{assay_name}{Character. Assay to pull expression from.
Default \code{"logcounts"}.}

\item{top_n}{Integer. Maximum marker genes per cluster shown when
\code{markers} is a \code{keraunos_markers} object.  Default \code{5L}.}

\item{scale}{Logical. Z-score mean expression per gene across clusters
before plotting.  Default \code{TRUE}.}

\item{min_expr}{Numeric. Expression threshold used to count a cell as
"expressing" the gene.  Default \code{0}.}

\item{dot_max_size}{Numeric. Maximum dot diameter (ggplot size units).
Default \code{6}.}

\item{palette}{Character vector of colours for the fill gradient.
\code{NULL} (default) uses a blue–white–red diverging palette.}

\item{title}{Character. Plot title.  Default \code{"Marker genes"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualises cluster marker genes as a dot plot where dot \strong{size}
encodes the fraction of cells expressing each gene and dot \strong{colour}
encodes scaled (or raw) mean expression.  Genes are ordered by source
cluster and dashed horizontal lines separate marker groups.
}
