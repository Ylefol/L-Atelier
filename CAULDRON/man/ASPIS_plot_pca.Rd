% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aspis_embedding.R
\name{ASPIS_plot_pca}
\alias{ASPIS_plot_pca}
\title{PCA scatter plot}
\usage{
ASPIS_plot_pca(
  sce,
  colour_by = "cluster",
  dimred = "PCA",
  point_size = 0.8,
  point_alpha = 0.6,
  palette = NULL,
  title = NULL,
  label_clusters = FALSE,
  label_size = 4,
  assay_name = "logcounts",
  ncol = NULL,
  show_legend = TRUE,
  legend_point_size = 4
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with the target reduced dimension
populated.}

\item{colour_by}{Character scalar or vector.  One or more \code{colData}
column names or gene names present in \code{rownames(sce)}.  A single
value returns a \code{ggplot}; a vector produces one panel per element
arranged in a grid (requires \code{gridExtra}).  Default \code{"cluster"}.}

\item{dimred}{Character.  Name of the \code{reducedDims} slot to plot.
Default \code{"PCA"}.  Set to a custom name (e.g. \code{"PCA_singler"})
when plotting a non-standard PCA — attach a \code{percentVar} numeric
vector as an attribute on that matrix to show variance on the axes.}

\item{point_size}{Numeric. Point size. Default \code{0.8}.}

\item{point_alpha}{Numeric. Point transparency (0–1). Default \code{0.6}.}

\item{palette}{Character vector or \code{NULL}.  For discrete variables:
a vector of colours (recycled as needed).  For continuous variables or
gene expression: a 2-element vector \code{c(low, high)}.
\code{NULL} (default) uses built-in palettes.}

\item{title}{Character or \code{NULL}.  Plot title.  \code{NULL}
auto-generates \code{"PCA — <colour_by>"}.}

\item{label_clusters}{Logical.  Overlay cluster centroid labels.  Only
applied when \code{colour_by} resolves to a discrete variable.
Default \code{FALSE}.}

\item{label_size}{Numeric.  Size of centroid labels.  Default \code{4}.}

\item{assay_name}{Character.  Assay used when \code{colour_by} is a gene.
Default \code{"logcounts"}.}

\item{ncol}{Integer or \code{NULL}.  Number of columns in the panel grid
when \code{colour_by} is a vector.  \code{NULL} uses
\code{min(length(colour_by), 3)}.}

\item{show_legend}{Logical.  Whether to display the colour legend.
Default \code{TRUE}.}

\item{legend_point_size}{Numeric.  Size of the coloured point in the
discrete legend.  Default \code{4}.}
}
\value{
A \code{ggplot} (single \code{colour_by}) or a \code{gtable} grid
(multiple \code{colour_by}).
}
\description{
Plots two principal components from a \code{reducedDims} slot, coloured by a
\code{colData} column or gene expression value.  Axis labels include the
percentage of variance explained when a \code{percentVar} attribute is
present on the reduced-dimension matrix (set automatically by
\code{\link{TALOS_run_pca}}, or by the caller for custom PCA objects).
}
