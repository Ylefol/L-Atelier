% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pandora_annotation.R
\name{PANDORA_annotate_sctype}
\alias{PANDORA_annotate_sctype}
\title{Marker-based cell type annotation via scType}
\usage{
PANDORA_annotate_sctype(
  sce,
  tissue,
  cluster_col = "cluster",
  db = NULL,
  assay_name = "logcounts",
  label_col = "sctype_label",
  verbose = TRUE
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with a \code{"logcounts"} (or
other) assay.}

\item{tissue}{Character. Tissue type string matching an entry in the
scTypeDB (case-sensitive).}

\item{cluster_col}{Character. \code{colData} column with cluster
assignments.  Default \code{"cluster"}.}

\item{db}{\code{NULL} (bundled \code{ScTypeDB_full.xlsx}), a file path to a
custom Excel or CSV, or a \code{data.frame} with columns
\code{tissueType}, \code{cellName}, \code{geneSymbolmore1} (positive
markers, comma-separated), \code{geneSymbolmore2} (negative markers,
comma-separated; \code{NA} = none).}

\item{assay_name}{Character. Assay to score. Default \code{"logcounts"}.}

\item{label_col}{Character. \code{colData} column for the result.
Default \code{"sctype_label"}.}

\item{verbose}{Logical. Print a summary. Default \code{TRUE}.}
}
\value{
SCE with \code{colData(sce)[[label_col]]} populated.
}
\description{
Scores clusters against a curated marker gene database to assign cell type
labels.  Annotation is cluster-level: expression is aggregated per cluster,
scored against each cell type's positive and negative markers (from the
bundled \code{ScTypeDB_full.xlsx}), and the highest-scoring type is assigned
to all cells in each cluster.
}
\details{
Use \code{\link{PANDORA_list_references}(source = "sctype")} to see
available tissues and cell type counts before running.
}
