% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/talos_embedding.R
\name{TALOS_run_pca}
\alias{TALOS_run_pca}
\title{Principal component analysis}
\usage{
TALOS_run_pca(
  sce,
  n_pcs = 50L,
  use_hvg = TRUE,
  assay_name = "logcounts",
  scale = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with a \code{"logcounts"} assay.}

\item{n_pcs}{Integer. Number of principal components to compute.
Default \code{50}.}

\item{use_hvg}{Logical. If \code{TRUE} (default), restricts to genes in
\code{metadata(sce)$hvg}.  Falls back to all genes with a warning if HVGs
are not found.}

\item{assay_name}{Character. Assay to use. Default \code{"logcounts"}.}

\item{scale}{Logical. Scale genes to unit variance before PCA.
Default \code{FALSE}.}

\item{verbose}{Logical. Print a summary. Default \code{TRUE}.}
}
\value{
SCE with \code{reducedDims(sce)[["PCA"]]} populated.
}
\description{
Runs PCA on the normalised \code{logcounts} assay, restricted to HVGs
selected by \code{\link{PYRI_select_hvg}} when available.  Results are
stored in \code{reducedDims(sce)[["PCA"]]}.
}
