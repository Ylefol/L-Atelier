% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keraunos_expression.R
\name{KERAUNOS_contrast_groups}
\alias{KERAUNOS_contrast_groups}
\title{Cell-level contrast between groups (quick path)}
\usage{
KERAUNOS_contrast_groups(
  sce,
  group_col,
  cluster_col = NULL,
  assay_name = "logcounts",
  test_type = c("wilcox", "t", "binom"),
  direction = c("any", "up", "down"),
  fdr_threshold = 0.05,
  top_n = 10L,
  verbose = TRUE
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with a \code{logcounts} (or other)
assay.}

\item{group_col}{Character. \code{colData} column defining the groups to
contrast (must have \eqn{\geq 2} levels).}

\item{cluster_col}{Character or \code{NULL}.  If \code{NULL} (default), a
single global contrast is performed.  If a \code{colData} column name is
provided, the contrast is run independently within each cluster.}

\item{assay_name}{Character. Assay to test. Default \code{"logcounts"}.}

\item{test_type}{Character. Test: \code{"wilcox"} (default), \code{"t"},
or \code{"binom"}.}

\item{direction}{Character. Direction of effect: \code{"any"} (default),
\code{"up"}, or \code{"down"}.}

\item{fdr_threshold}{Numeric. FDR cut-off for the tidy summary.
Default \code{0.05}.}

\item{top_n}{Integer. Maximum top hits per group/cluster in the tidy table.
Default \code{10L}.}

\item{verbose}{Logical. Print a summary and pseudoreplication warning.
Default \code{TRUE}.}
}
\value{
A \code{keraunos_contrast} object (list) with:
\itemize{
\item \code{$results} — named list of findMarkers DataFrames (one per
group if global; named list \code{[[cluster]][[group]]} if clustered).
\item \code{$top} — tidy data.frame of top hits.
\item \code{$params} — list of parameters used.
}
}
\description{
Tests for differentially expressed genes between two (or more) groups at
the single-cell level using \code{scran::findMarkers()}.  Can run globally
or within each cluster independently.
}
\details{
\strong{Important:} This is a cell-level test.  With multiple biological
samples per group, individual cells from the same sample are not independent
— this is the pseudoreplication problem.  For a statistically rigorous
multi-sample comparison, use \code{\link{KERAUNOS_de_pseudobulk}} instead.
Use this function for exploratory analysis or single-sample datasets.
}
