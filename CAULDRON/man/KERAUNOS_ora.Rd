% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keraunos_expression.R
\name{KERAUNOS_ora}
\alias{KERAUNOS_ora}
\title{Over-representation analysis via gprofiler2}
\usage{
KERAUNOS_ora(
  genes,
  background = NULL,
  organism = "hsapiens",
  sources = c("GO:BP", "GO:MF", "GO:CC", "KEGG", "REAC"),
  fdr_threshold = 0.05,
  verbose = TRUE
)
}
\arguments{
\item{genes}{Character vector of gene symbols (or Ensembl IDs, Entrez IDs,
etc. — whatever gprofiler2 accepts for the chosen organism).}

\item{background}{Character vector of background genes, or \code{NULL}
(default) to use the gprofiler2 reference genome as background.}

\item{organism}{Character. gprofiler2 organism code.
Default \code{"hsapiens"}.  Use \code{"mmusculus"} for mouse, etc.
See \code{gprofiler2::gost()} for full list.}

\item{sources}{Character vector. Databases to query.
Default \code{c("GO:BP", "GO:MF", "GO:CC", "KEGG", "REAC")}.}

\item{fdr_threshold}{Numeric. FDR threshold for \code{$significant}.
Default \code{0.05}.}

\item{verbose}{Logical. Print a summary. Default \code{TRUE}.}
}
\value{
A \code{keraunos_ora} object (list) with:
\itemize{
\item \code{$results} — full gprofiler2 result data.frame (ordered by
p-value).
\item \code{$significant} — filtered to \code{fdr_threshold}.
\item \code{$params} — list of parameters used.
}
}
\description{
Tests whether a set of genes is enriched for annotated biological terms
using \code{gprofiler2::gost()}.  Supports GO terms, KEGG, Reactome,
and other databases available through g:Profiler.
}
