% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pandora_annotation.R
\name{PANDORA_assign_labels}
\alias{PANDORA_assign_labels}
\title{Manually assign cell type labels to clusters}
\usage{
PANDORA_assign_labels(
  sce,
  labels,
  cluster_col = "cluster",
  label_col,
  verbose = TRUE
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment}.}

\item{labels}{Named character vector.  Names are cluster IDs (must match
levels in \code{cluster_col}); values are cell type labels.
Example: \code{c("0" = "T cell", "1" = "B cell", "2" = "Myeloid")}.}

\item{cluster_col}{Character. \code{colData} column containing cluster IDs.
Default \code{"cluster"}.}

\item{label_col}{Character. Name of the new \code{colData} column to create.
Must be supplied explicitly; no default to prevent accidental overwrites.}

\item{verbose}{Logical. Print a summary. Default \code{TRUE}.}
}
\value{
SCE with \code{colData(sce)[[label_col]]} populated.
}
\description{
Maps cluster IDs to user-defined cell type labels using a named character
vector, storing the result as a new \code{colData} column.  Clusters
without a matching entry in \code{labels} receive \code{NA}.
}
