% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/talaria_logging.R
\name{TALARIA_generate_log_filename}
\alias{TALARIA_generate_log_filename}
\title{Generate a timestamp-based log filename}
\usage{
TALARIA_generate_log_filename(
  log_dir = NULL,
  prefix = "log",
  extension = ".log",
  format = c("compact", "readable")
)
}
\arguments{
\item{log_dir}{Character or \code{NULL}.  Directory in which to place the
file.  \code{NULL} (default) returns just the filename with no path.}

\item{prefix}{Character.  Filename prefix.  Default \code{"log"}.}

\item{extension}{Character.  File extension.  Default \code{".log"}.}

\item{format}{Character.  Timestamp style:
\describe{
\item{\code{"compact"}}{Default. \code{YYYY_MM_DD__HH_MM} — filesystem-safe,
no seconds (e.g. \code{log_2026_03_05__14_30.log}).}
\item{\code{"readable"}}{\code{YYYY-MM-DD_HH-MM-SS} — includes seconds
(e.g. \code{log_2026-03-05_14-30-52.log}).}
}}
}
\value{
Character string — full file path if \code{log_dir} is provided,
otherwise just the filename.
}
\description{
Creates a log filename using the current date and time, suitable for
automatic naming so log files sort chronologically on disk.
}
