% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/talos_graph.R
\name{TALOS_cluster}
\alias{TALOS_cluster}
\title{Graph-based clustering}
\usage{
TALOS_cluster(
  sce,
  method = c("leiden", "louvain"),
  resolution = 1,
  cluster_col = "cluster",
  seed = 42L,
  verbose = TRUE
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with \code{metadata(sce)$snn_graph}
populated (i.e. \code{\link{TALOS_build_graph}} must have been run first).}

\item{method}{Character. \code{"leiden"} (default) or \code{"louvain"}.
Leiden generally produces better-connected, more reproducible partitions.}

\item{resolution}{Numeric. Resolution parameter controlling cluster
granularity.  Higher values yield more, smaller clusters. Default
\code{1.0}. Applied to both methods.}

\item{cluster_col}{Character. \code{colData} column name for storing labels.
Default \code{"cluster"}.}

\item{seed}{Integer. Random seed. Default \code{42L}.}

\item{verbose}{Logical. Print a summary. Default \code{TRUE}.}
}
\value{
SCE with cluster labels in \code{colData(sce)[[cluster_col]]}.
}
\description{
Applies Leiden or Louvain community detection to the SNN graph built by
\code{\link{TALOS_build_graph}}.  Cluster labels are stored in
\code{colData(sce)[[cluster_col]]}.
}
